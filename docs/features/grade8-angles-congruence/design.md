# זוויות וחפיפת משולשים — Design

## Overview
כרטיס נושא נוסף ברשימת עומר, בדיוק כמו ששת הנושאים הקיימים — לוחצים ונכנסים ישר
לתרגול (levels-as-practice), אותה תיבת שאלה, אותם שני רמזים, אותו הסבר אחרי טעות.
מה שחדש: תיאור מילולי-עברי עם אותיות לטיניות מבודדות (`A`, `B`, `∡C`, `α`) בתוך
המשפט, וחמש צורות ציור חדשות שמצטרפות לספריית הדיאגרמות הקיימת (`more-diagrams`) —
אחת או שתיים לכל תת-תחום, באותו מיקום ובאותה שפה חזותית שכבר קיימת היום למשולש
הפיתגורס.

## Screens / States

### כרטיס הנושא (`TopicPicker`) — ללא שינוי במבנה
כרטיס נוסף בין ששת הכרטיסים הקיימים של עומר, באותה צורה בדיוק — כותרת בלבד, בלי
אייקון (אף כרטיס נושא היום לא נושא אייקון). מיקום מוצע: אחרי "משפט פיתגורס", לפני
"פונקציה קווית" — משאיר את שני נושאי הגיאומטריה זה לצד זה.

### לחיצה על הכרטיס — ישר לתרגול, בלי מסך רמות
**עדכון (סבב ב׳):** בעקבות בקשת המשתמש המפורשת ("אני רוצה מחולל עכשיו. אין יותר
רמות לבחירה"), הנושא נכנס בדיוק כמו ששת נושאי כיתה ח׳ האחרים — `docs/features/
levels-as-practice/design.md` הוא המפרט המחייב לזרימה הזו, לא חזרה עליו כאן: אין
מסך `LevelPicker` (קל/בינוני/מתקדם) בכלל, אין מחוון קושי גלוי לילד/ה, ותרגול הוא
`20` שאלות (לא `10`). הקושי מתאים את עצמו בשקט ברקע. שום דבר בסעיף הזה לא ספציפי
לנושא הזה — זה בדיוק אותו דפוס שכבר קיים בכל נושא מסתגל אחר.

### מסך התרגול — ללא שינוי מבני
`Practice.tsx` נשאר זהה: תיבת שאלה, שני כפתורי רמז, הקראה, "הבא". התוכן החדש הוא
בתוך השדות הקיימים (`prompt`, `hints`, `steps`, `analogy`) — לא רכיב חדש.

### תיבת השאלה — תיאור מילולי עם אותיות לטיניות מבודדות
כמו בכל נושא אחר, ה`-prompt` הוא משפט עברי עם המספרים/ביטויים בגרשיים אחוריים
(`segmented()` המנגנון הקיים). כאן הביטויים בגרשיים כוללים גם שמות זוויות ונקודות
לטיניות, לא רק מספרים:

```
בזווית ישרה `∡ABC` יש שתי זוויות: `∡ABD = 65°` ו-`∡DBC`. מה גודל `∡DBC`?
משולש `ABC` חופף למשולש `EDC` (`ABC ≅ EDC`). `AB = 7`. מה אורך `DE`?
במשולש `ABC` שווה-השוקיים (`AB = AC`), זווית הראש `∡A = 40°`. מה גודל זווית הבסיס `∡B`?
```

זה משתמש באותו מנגנון בידוד קיים (`unicode-bidi: isolate`, גרשיים אחוריים) — לא
מנגנון חדש, רק תוכן חדש בתוכו.

### דיאגרמה בהסבר — חמש צורות חדשות, באותו מיקום שכבר קיים
בראש קופסת "איך פותרים?", לפני השלבים, בדיוק כמו משולש הפיתגורס וארבע הצורות
האחרות מ-`more-diagrams`: רוחב מרבי `280` פיקסלים, קו בצבע הטקסט הראשי, `direction:
ltr` על ה-`<svg>`, כיתוב עברי אחד מתחתיה. ה**חדש שנקבע** מתאים בהמשך.

#### 1 · זווית ישרה/השלמה — שני קרניים עם קשת
`בזווית ישרה 130° ו-50°, מה הזווית השלישית?` / `∡ABD=65°, ∡DBC=?`

```
        D
       ╱  ╲
      ╱ 65°╲  ?
  ───●───────●───
     B       C
   (A משמאל ל-B, על אותו קו)
```

הזווית הנתונה מסומנת בקשת עם המספר בתוכה; הזווית המבוקשת מסומנת בקשת ריקה עם `?`.
קו הבסיס ישר תמיד (`180°`), כדי שהעין תראה מיד שיש כאן שלם שמתחלק.

#### 2 · משולש עם זווית מסומנת — סכום זוויות / זווית חיצונית
`∡A=80°, ∡B=40°, מה ∡C?` / `זווית חיצונית ב-C, כמה היא?`

```
        A
       ╱ 80°╲
      ╱      ╲
     ╱        ╲
    ╱40°      C╲──── (המשך הצלע, לזווית חיצונית)
   B─────────────
```

שתי הזוויות הנתונות מסומנות בקשת עם מספר; השלישית (פנימית או חיצונית, לפי השאלה)
מסומנת בקשת ריקה. לזווית חיצונית, הצלע ממשיכה מעבר לקודקוד בקו מקווקו קצר — כמו
"המשך הצלע" בציור, לא רק תיאור מילולי.

#### 3 · שני ישרים מקבילים וישר חותך
`הזווית בין a ל-c היא 110°. מה α בין b ל-c?`

```
   a ───────────╱────────
              ╱ 110°
             ╱
   b ───────╱────α────────
           ╱
          c
```

שני קווים אופקיים מקבילים (מסומנים בחיצי מקבילות קטנים `▷▷` על שניהם, כמו בסימון
הגיאומטרי המקובל), וישר חותך אלכסוני. הזווית הנתונה מסומנת עם המספר, המבוקשת עם
`?`/`α`.

#### 4 · שני משולשים חופפים — סימוני התאמה
`ABC ≅ EDC. AB=7, DE=?`

```
      A                    E
     ╱|╲                  ╱|╲
    ╱ | ╲                ╱ | ╲
   ╱7 |  ╲              ╱? |  ╲
  ╱   |   ╲            ╱   |   ╲
 B────C────•          D────C────•
```

שני המשולשים זה לצד זה, חולקים את קודקוד ה-`C` המשותף (כמו בשאלות המקוריות). צלעות
מתאימות מסומנות באותו סימון קווים קצרים (`/` אחד לזוג ראשון, `//` לזוג שני) על שני
המשולשים גם יחד — כך שרואים איזה צלע תואמת לאיזו בלי לקרוא את שם ההתאמה. הצלע/זווית
הנתונה עם מספר, המבוקשת עם `?`.

#### 5 · משולש שווה-שוקיים עם תיכון
`AB=AC. ∡A=40°. התיכון AD. מה ∡ADB?`

```
        A
       ╱|╲
      ╱ | ╲
     ╱40°  ╲
    ╱  |    ╲
   B───D─────C
```

שוקיים מסומנות בקו קצר זהה (`/`), הזווית בראש עם מספר, קטע `AD` מצויר עד הבסיס.
כשהשאלה עוסקת בזווית `∡ADB` (ישרה, כי התיכון הוא גם גובה) — הזווית הישרה שם מסומנת
בריבוע קטן, **בדיוק** כמו הריבוע שכבר מסמן זווית ישרה במשולש הפיתגורס. זו אותה שפה
חזותית, לא סימון חדש.

### מצב ללא ציור
אין — לכל שאלה בנושא הזה יש דיאגרמה מתאימה מבין החמש, בדיוק כמו שלכל שאלת פיתגורס
יש את דיאגרמת המשולש ישר-הזווית.

## Copy

שורת כיתוב אחת מתחת לכל דיאגרמה, לפי אותה תבנית של שאר `more-diagrams` — **מה
רואים**, לא החישוב:

| צורה | כיתוב |
|---|---|
| זווית ישרה/השלמה | <code>קו ישר הוא \`180°\`. \`65°\` מהם ידועים, והשאר הוא \`115°\`</code> |
| משולש — סכום זוויות | <code>סכום הזוויות במשולש הוא תמיד \`180°\`</code> |
| משולש — זווית חיצונית | <code>הזווית החיצונית שווה לסכום שתי הזוויות הפנימיות הרחוקות ממנה</code> |
| ישרים מקבילים | <code>שני קווים מקבילים וישר חותך — הזוויות המתאימות/מתחלפות שוות</code> |
| משולשים חופפים | <code>הסימונים הזהים מראים אילו צלעות וזוויות שוות בין שני המשולשים</code> |
| שווה-שוקיים ותיכון | <code>במשולש שווה-שוקיים, התיכון מקודקוד הראש הוא גם גובה — הוא יוצר זווית ישרה בבסיס</code> |

הרמזים והשלבים (`hints`, `steps`) בכל שאלה כתובים באותו סגנון "מלמד לא מודיע" של
שאר הנושאים — לדוגמה, לא "התשובה היא 115°" אלא:

- רמז 1: `"קו ישר הוא תמיד 180°"`
- רמז 2: `"מורידים את הזווית הידועה מ-180°"`
- שלב: `{ label: "מעבירים: 180° − 65°" }`, `{ label: "", math: "180 − 65 = 115" }`

לזווית חיצונית:
- רמז 1: `"הזווית החיצונית שווה לסכום שתי הזוויות הפנימיות הרחוקות ממנה"`
- רמז 2: `"מחברים את שתי הזוויות שאינן צמודות לזווית החיצונית"`

לחפיפה:
- רמז 1: `"שני משולשים חופפים — צלעות וזוויות מתאימות שוות"`
- רמז 2: `"מסתכלים על סדר האותיות בחפיפה כדי לדעת איזו צלע תואמת לאיזו"`

לשווה-שוקיים:
- רמז 1: `"במשולש שווה-שוקיים זוויות הבסיס שוות זו לזו"`
- רמז 2: `"סכום הזוויות במשולש הוא 180°, ושתי זוויות הבסיס שוות"`

## Interaction Flow
1. עומר לוחץ על כרטיס "זוויות וחפיפת משולשים" ברשימת הנושאים שלו.
2. עובר **ישר** לתרגול — אין מסך רמות לעצור בו. שאלה ראשונה נבחרת בקושי ההתחלתי
   (כמו כל נושא מסתגל אחר).
3. שאלה → תשובה → אם שגויה, קופסת "איך פותרים?" נפתחת עם הדיאגרמה בראשה → אין מה
   ללחוץ בדיאגרמה עצמה → הקראה אומרת את שורת הכיתוב לפני השלבים → "הבא" מנקה הכל.
4. אחרי כל תשובה, הקושי של השאלה **הבאה** עשוי לזוז (עולה/יורד) בלי סימן חזותי —
   בדיוק כמו בכל נושא מסתגל אחר.
5. אחרי `20` שאלות — מסך תוצאה רגיל, `X מתוך 20`.

## Accessibility / RTL notes
- **כל אות/שם זווית לטיני (`A`, `∡ABC`, `α`, `40°`) עובר דרך אותו מנגנון בידוד קיים
  — גרשיים אחוריים ב-`prompt`, ש-`segmented()` הופך ל-`unicode-bidi: isolate`.** זה
  הנושא הראשון שבו התוכן המבודד הוא לא רק מספרים וסימני חשבון אלא גם אותיות שמות
  (`A`, `B`, `C`) וסימן זווית (`∡`) — צריך לוודא ש`promptSegments` לא מניח שהתוכן
  בגרשיים הוא תמיד ביטוי מספרי טהור. תשומת לב ישירה ל-tech-lead: לבדוק את ה-regex/
  לוגיקת הפיצול מול המחרוזות האלה, לא רק להניח שזה עובד כי זה "עוד טקסט בגרשיים".
- **`direction: ltr` על כל `<svg>` וכל `<text>`, בדיוק כמו כל דיאגרמה קיימת** —
  תוויות קודקוד (`A`, `B`, `C`) בתוך הציור לא מתהפכות ולא מסתובבות עם הצורה.
- **סימון "מה ידוע מול מה מבוקש" הוא צורני, לא צבעוני** — קשת עם מספר מול קשת עם
  `?`; קווי סימון (`/`, `//`) על צלעות מתאימות; ריבוע קטן לזווית ישרה. תואם את הכלל
  הקיים בפרויקט ש"צבע אינו נושא משמעות לבדו".
- כיוון הישרים המקבילים בדיאגרמה 3 הוא אופקי (שמאל-ימין), התואם לכיוון שבו כל ציר/
  ישר אחר באפליקציה כבר מצויר (ראו דיאגרמת הפונקציה הקווית ב-`more-diagrams`).

## Acceptance Criteria Check
- נושא חדש בשם "זוויות וחפיפת משולשים" ברשימת עומר: **yes** — כרטיס נוסף, ראו
  Screens/States.
- הנושא מסתגל, בלי מסך רמות (סבב ב׳): **yes** — ראו Screens/States, "לחיצה על
  הכרטיס", ו-Interaction Flow. אותו דפוס בדיוק כמו ששת נושאי כיתה ח׳ האחרים.
- תת-תחום זוויות (השלמה, זווית חיצונית, סכום זוויות, ישרים מקבילים): **yes** —
  דיאגרמות 1–3, נוסחי prompt לדוגמה, כיתוב.
- תת-תחום חפיפת משולשים: **yes** — דיאגרמה 4, נוסח prompt וכיתוב.
- תת-תחום משולש שווה-שוקיים: **yes** — דיאגרמה 5, נוסח prompt וכיתוב.
- `analogy` לכל שאלה: **partial** — זה שדה נתונים לא עיצובי; ה-design מוודא שהתיאור
  המילולי בכל דוגמה כאן ניתן לליווי אנלוגיה מהחיים (כמו "שני כבישים מקבילים וכביש
  חוצה" לדיאגרמה 3), אבל כתיבת ה-`analogy` בפועל לכל שאלה היא עבודת `developer`.
- כיווניות נכונה: **yes** — ראו Accessibility/RTL notes, כולל האזהרה הספציפית
  ל-tech-lead על אותיות שמות בתוך הבידוד.
- זמין מתרגול רגיל ומהמחברת, מופיע בהיסטוריה: **yes** — אין מסך חדש; זה אותו נתיב
  שכל נושא כיתה ח׳ כבר עובר בו היום (`TopicPicker` → `Practice` → `progress.ts`).

## עדכון עיצוב — סבב ג׳ (2026-09-05, משוב על התצוגה המקדימה)

המשתמש נתן משוב על שמונה שאלות לדוגמה. שני פריטים הם החלטות עיצוב רוחביות; שאר
הפריטים הם שינויי ניסוח ממוקדים. ראו product-spec.md, "עדכון קריטריוני קבלה —
סבב ג׳", להקשר המלא.

### הדיאגרמה עוברת לצד השאלה עצמה — לא רק בהסבר אחרי טעות

**התקדים המחייב הוא `docs/features/grade2-clock/design.md`**, לא המצאה חדשה: שם
כבר הוכרע בדיוק אותו מעבר (ציור השעון מוצג עם השאלה, נשאר על המסך אחרי שהתשובה
נבדקת, וקופסת "איך פותרים?" לא מציגה עוד עותק שלו). אותו עיקרון בדיוק כאן, לכל
שנים-עשר דפוסי השאלה:

- **הציור עצמו** (הקווים, הקשתות, המספר הידוע, סימן ה-`?`) מופיע ליד תיבת השאלה
  (`.problem-box`) מלמעלה או מתחתיה — כמו `clockSlot` ב-`Practice.tsx` — גם במסך
  הרגיל וגם בכותרת המחברת במסך מלא (`topSlot`), בדיוק כמו שהשעון עושה היום.
- **שורת הכיתוב המילולית** (למשל "קו ישר הוא `180°` שלם. `65°` מהם כבר ידועים,
  והשאר הוא הזווית המבוקשת") **לא** עוברת לצד השאלה — היא בעצם אומרת את הכלל
  ואת השיטה, מה שהיה הופך את שני הרמזים הקיימים למיותרים אם היה מוצג מראש. היא
  נשארת בדיוק במקומה היום: בתוך קופסת "איך פותרים?", אחרי תשובה שגויה, **בלי**
  להציג עוד עותק של הציור עצמו לצידה (אותו עיקרון "בלי עותק כפול" של clockFace) —
  רק את המשפט, מתחת לציור שכבר על המסך.
- ה-`aria-label` על הציור (לפני תשובה) מתאר רק את **הצורה** (למשל "איור: קו ישר
  עם שתי זוויות, אחת מסומנת `65°` והשנייה בסימן שאלה") — לא את הכלל/השיטה, כדי
  שהקראה למי שלא רואה תיתן בדיוק את אותו יתרון/חיסרון שרואה מקבל: את התמונה,
  לא את הפתרון.
- זה חל על **כל** שנים-עשר הדפוסים בנושא הזה (לא רק חלקם) — גיאומטריה היא תחום
  חזותי, ואין סיבה שדפוס אחד יקבל את היתרון והשני לא.

### שיעור קצר "למה זה נכון" — בתוך מסך השיעור הקיים

`docs/features/topic-lesson/design.md` כבר קובע: לנושא אדפטיבי, מסך השיעור
מציג דוגמה פתורה אחת לכל דרגת קושי, בדפדוף עמוד-אחר-עמוד, עם אותו פאנל הסבר
שקיים בתרגול. שני עמודים ספציפיים מקבלים תוספת — לא עמוד/מסך חדש:

- **עמוד "זווית חיצונית"**: אחרי המשפט/הרמזים/השלבים הקיימים, פסקה קצרה נוספת
  תחת כותרת-משנה **"למה זה נכון?"**, לפני כפתור "הבא →"/"→ לתרגול".
- **עמוד "זוויות מתאימות" (ישרים מקבילים)**: אותו דבר, פסקה תחת אותה כותרת-משנה.
- **חשוב לתת ל-tech-lead כהערה ישירה:** מתוך שני הדפוסים בתת-תחום "משולש —
  זוויות" (סכום זוויות / זווית חיצונית), ומתוך שלושת הדפוסים בתת-תחום "ישרים
  מקבילים" (מתאימות/מתחלפות/חד-צדדיות), רק דפוס אחד מכל תת-תחום מקבל את הפסקה
  הזו (זווית חיצונית; זוויות מתאימות בלבד — לא מתחלפות ולא חד-צדדיות). כדי
  שהפסקה תופיע בפועל במסך השיעור, בחירת הדוגמה לעמוד השיעור של אותה דרגת קושי
  צריכה להעדיף במפורש את הדפוס הזה מתוך הדרגה, לא דפוס אקראי מתוכה — אחרת יש
  סיכוי שהעמוד יציג את הדפוס האחר ("סכום זוויות"/"מתחלפות") והפסקה פשוט לא
  תופיע אף פעם. זו הכרעת מימוש (tech-lead/developer), לא עיצוב, אבל בלעדיה
  קריטריון הקבלה נכשל בשקט.
- **הקראה**: כפתור ה-🔊 הקיים בראש עמוד השיעור כבר מקריא הכל ברצף (שאלה, רמזים,
  הסבר) — הפסקה החדשה נוספת לאותו רצף, בסופו, בלי כפתור הקראה נפרד משלה.
- **לא הוכחה פורמלית**: אין "נתון/צ״ל/הוכחה", אין שמות משפטים באנגלית או עברית
  ("משפט תלס" לא מוזכר בטקסט המוצג לילד — הוא רק ההקשר שבו המשתמש הזכיר את
  זה, לא ניסוח מבוקש). שתי-שלוש משפטים בעברית פשוטה, באותו רישום כמו שאר התוכן
  הלימודי באפליקציה.

### סימון חפיפת משולשים — כל ההתאמות, לא רק זו שנשאלת

הדיאגרמה של חפיפת משולשים (הדפוס הרביעי, סעיף "4 · שני משולשים חופפים" למעלה)
מסמנת כרגע רק את הצלע/הזווית שהשאלה שואלת עליה. עכשיו מסמנת **את כל שש
ההתאמות** (שלוש צלעות, שלוש זוויות) בין `ABC` ל-`EDC`, לפי המוסכמה הגיאומטרית
הרגילה — מספר סימונים שונה לכל זוג מתאים, כדי שרואים בבירור אילו חלקים
"הולכים ביחד" בלי לקרוא את סדר האותיות בחפיפה:

- `AB↔ED`: סימון קו אחד (`/`) על שתי הצלעות.
- `BC↔DC`: סימון קו כפול (`//`).
- `CA↔EC`: סימון קו משולש (`///`).
- `∡A↔∡E`: קשת יחידה על שתי הזוויות.
- `∡B↔∡D`: קשת כפולה.
- `∡C↔∡C`: קשת משולשת.

**רק ההתאמה שהשאלה עוסקת בה** מקבלת גם תווית מספר/`?` (בדיוק כמו היום); שאר
חמש ההתאמות מקבלות את סימון הקווים/הקשתות בלבד, בלי מספר — כי ערכן לא נתון
ולא מבוקש, רק העובדה שהן שוות זו לזו. זו בדיוק המוסכמה שמורים באמת משתמשים
בה בלוח, לא סימון מומצא.

### ניסוחים

**זווית משלימה ל-`90°` (סעיף "1 · זווית ישרה/השלמה" למעלה, המקרה של `90°`
ולא `180°`):** במקום לקרוא לזה רק "שתי זוויות משלימות", ה-`prompt` קורא
לזווית השלמה בשמה ומציג את המושג "נצבים":

> `בין שני ישרים נצבים (זווית ישרה, \`90°\`), שתי זוויות סמוכות. אחת מהן \`${known}°\`. מה גודל השנייה?`

רמז 1 מתעדכן בהתאם: `"שני ישרים נצבים יוצרים ביניהם זווית ישרה — \`90°\`"`
(רמז 2, "מורידים את הזווית הידועה מ-`90`", ללא שינוי).

**סכום זוויות במשולש (סעיף "2" למעלה):** שלב הפתרון מציג את הסוגריים
במפורש, לא שני חישובים נפרדים:

> שלב 1: `"סכום הזוויות במשולש הוא \`180°\`"`
> שלב 2: `"מחברים את שתי הזוויות הידועות בתוך סוגריים, ואז מורידים מ-\`180\`"`
> שלב 3 (`math`): `` `180 − (${angleA} + ${angleB}) = ${180 - angleA - angleB}` ``

השלב האחד המשולב (סוגריים ← תוצאה) מחליף את שני שלבי ה-`math` הנפרדים
שהיו קודם (חיבור, ואז חיסור) — הסוגריים **הם** הצעד המלומד כאן, לא רק תוספת
ויזואלית לתוצאה שכבר הייתה נכונה.

## Accessibility / RTL notes (תוספת סבב ג׳)
- הציור שמוצג עם השאלה (לפני תשובה) חייב לעבוד גם במסך מלא (מצב מחברת), באותו
  מקום שהשעון כבר עושה זאת (`topSlot`) — לא רק במסך הרגיל.
- ה-`aria-label` "לפני תשובה" ו"אחרי תשובה" (שורת הכיתוב) הם שני מחרוזות שונות
  במכוון (ראו למעלה) — לא לאחד אותם ל-`aria-label` אחד שחוזר על עצמו, ולא
  לגרום לקורא-מסך "לשמוע את התשובה" לפני שרואה-המסך רואה אותה.
- סימוני ההתאמה בחפיפה (קו יחיד/כפול/משולש, קשת יחידה/כפולה/משולשת) הם צורניים
  בלבד, לא צבעוניים — עקבי עם העיקרון הקיים במסמך הזה.

## Acceptance Criteria Check (עדכון סבב ג׳)
- הדיאגרמה מוצגת יחד עם השאלה, לכל 12 הדפוסים: **yes** — ראו למעלה, וההערה
  המפורשת לגבי אי-הצגת עותק כפול.
- שיעור קצר "למה זה נכון" לזווית חיצונית ולזוויות מתאימות: **yes**, בתנאי
  שבחירת הדוגמה בעמוד השיעור מעדיפה את הדפוס הנכון בתוך הדרגה — ראו ההערה
  ל-tech-lead למעלה; אם זה לא ייושם, הקריטריון נכשל בשקט (השיעור עלול להציג
  את הדפוס האחר בדרגה ולעולם לא להראות את הפסקה).
- חפיפת משולשים מסמנת את כל שש ההתאמות: **yes** — ראו סעיף הסימון למעלה.
- ניסוח "זווית ישרה"/"נצבים" ל-`90°`, סוגריים מפורשים לסכום זוויות: **yes** —
  ראו סעיף "ניסוחים".

## Open Questions
None.
