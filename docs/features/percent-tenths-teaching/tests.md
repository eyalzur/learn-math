# רצועת האחוזים מתחלקת לפי האחוז שבשאלה, וההסבר אומר למה — Tests

## Coverage

| קריטריון קבלה (product-spec.md) | בדיקה |
|---|---|
| הרצועה מתחלקת למספר החלקים שהאחוז שקול לו, לא למספר קבוע | `tests/e2e/more-diagrams-wave2.spec.ts` › `the strip divides into the parts the question's own percent reduces to` — בודקת `10%`→`9` קווים (`10` חלקים), `50%`→`1` קו (`2` חלקים), `25%`→`3` קווים (`4` חלקים), `20%`→`4` קווים (`5` חלקים) |
| `30%`/`75%` (המחלק המשותף שונה מהאחוז עצמו) מחושבים נכון | אותה בדיקה — `30%`→`9` קווים (`10` חלקים, `3` מלאים), `75%`→`3` קווים (`4` חלקים, `3` מלאים). זה בדיוק המקום שהכי קל לטעות בו (לחשב לפי `X` ולא לפי `gcd(X,100)`), ולכן שני המקרים האלה מסומנים בפירוש בקוד הבדיקה |
| ההסבר אומר את השקילות במפורש ומנמק אותה | נבדק ברמת התוכן — `tests/e2e/content.spec.ts`'s existing RULE `"a written step's own arithmetic checks out"` רץ (ולא נכשל) על שני השלבים החדשים בכל `20` השאלות; שני השלבים הם `label`-בלבד (בלי `math`), כך שהכלל לא מנסה לבדוק אותם כחשבון — נכון ומכוון, מתועד ב-architecture.md |
| הרמז/השלבים החדשים לא דולפים את התשובה, ולא משאירים מספר לא מסומן | הכלל הקיים `"keeps its hints to exactly two, without giving the answer away"` ב-`content.spec.ts` רץ אוטומטית על כל `330` השאלות, כולל `20` המעודכנות — עבר בריצה המלאה |
| חל על כל השאלות בתבנית "`X%` מ-`Y`?", לא רק `10%` | הבדיקה החדשה מכסה את כל `6` השערים הקיימים בנתונים (`10/20/25/30/50/75%`) |
| הרצועה לא מופיעה לפני תשובה, ומחיר-עלה עדיין רץ נכון | בדיקות קיימות ולא נגועות ב-`more-diagrams-wave2.spec.ts`: `"no percent strip before the question is answered"`, `"a price increase extends the strip past its own whole"` — עדיין עוברות (ענף ה"עלייה" לא השתנה, `denominator` לא מוגדר שם) |

## How to run
`npm run test:e2e`

## Status
עבר — ריצה נקייה בודדת, `230/230` (`2026-08-19`), כולל `npm run build` ו-`npm run lint` נקיים לפני כן. הריצה כללה את כל הסוויטה, לא רק את הבדיקה החדשה — ללא רגרסיה בשום פיצ'ר אחר.

## Notes
- הבדיקה הישנה (`"the strip carries guide lines at every quarter"`, שהניחה `3` קווים קבועים) הוחלפה — לא נמחקה סתם, אלא נבנתה מחדש כדי לבדוק את ההתנהגות החדשה (חלוקה לפי המכנה של האחוז הספציפי) על פני `6` השערים.
- לא נדרש שינוי ב-`content.spec.ts` עצמו — הכללים הקיימים כבר רצים על השאלות המעודכנות ומכסים את מה שצריך (בלי דליפת תשובה, בלי חשבון שגוי בשלב כתוב).
- `src/data/adaptivePercent.ts` (מ-`PR #48`, טרם מוזג) לא קיים על הבראנץ' הזה — אין בדיקות עליו כאן, כמתועד ב-architecture.md's Risks כמשימת המשך לא חוסמת.
