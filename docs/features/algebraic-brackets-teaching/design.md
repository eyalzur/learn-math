# פתיחת סוגריים בביטויים אלגבריים — Design

## Overview
שינוי תוכן בלבד בתוך פאנל ה"הסבר" הקיים (אותו פאנל שכל שאלה כבר מציגה — hints לפני
תשובה, steps + analogy אחרי טעות). אין מסך חדש, אין רכיב חדש, אין שינוי בפריסה —
רק החלפת מחרוזות ה-hints וה-steps בשישה מקומות: מחולל `openBrackets`, ארבע השאלות
הכתובות המקבילות ב-`grade8.ts`, ומחולל `combineLikeTerms`.

## Screens / States
אין שינוי מסך. הפאנל הקיים (methodSentence-אם-יש + שני hints + steps + analogy)
ממשיך להופיע באותם מקומות בדיוק: לפני תשובה (hints זמינים דרך כפתור "רמז"), ואחרי
תשובה שגויה (steps + analogy נחשפים). מסך השיעור החדש (topic-lesson, בפיתוח
במקביל) יציג את אותו תוכן דרך אותו נתיב — אין כאן שום דבר ספציפי למסך.

## Copy

**תיקון אחרי שלב QA:** הניסוח המקורי של hint 2 בכל חמשת המקומות הבאים כתב את `${answer}`
כמספר מוחשי ("... `${answer}` הוא המספר החופשי") — זה בפועל מוסר את התשובה לפני שהתלמיד/ה
ניסה, וכלל התוכן הקיים "keeps its hints to exactly two, without giving the answer away"
(ב-`tests/e2e/content.spec.ts`) תפס את זה על כל שאלה מהדפוס. הניסוח למטה מתקן זאת: hint 2
נשאר נוסחה עם המספרים הנתונים (`${a}` כפול `${b}`) בלי לכתוב את `${answer}` בעצמו — התשובה
עצמה ממשיכה להיחשף רק ב-steps, אחרי טעות, כמו תמיד.

### `openBrackets` (המחולל האדפטיבי, `src/data/adaptiveExpressions.ts`)
עבור `A(v + B)`, `answer = A × B`:

- Prompt: ללא שינוי — `` פתחו סוגריים: `${a}(${v} + ${b})`. מה המספר החופשי? ``
- hints:
  1. `כופלים את מה שמחוץ לסוגריים בכל אחד מהאיברים שבפנים`
  2. `` `${a}` הוא המקדם (המספר שכופל את `${v}`), ו-`${a}` כפול `${b}` הוא המספר החופשי ``
- steps:
  1. `כופלים את המספר שמחוץ לסוגריים בכל איבר בסוגריים`
  2. `` כופלים את המשתנה: `${a} × ${v} = ${a}${v}` ``
  3. `` כופלים את המספר: `${a} × ${b} = ${answer}` ``
  4. `` הביטוי הפתוח: `${a}${v} + ${answer}` ``
- analogy: ללא שינוי.

### `g8-expressions-m1` — `3(x + 2)`, answer 6
- hints:
  1. `כופלים את מה שמחוץ לסוגריים בכל אחד מהאיברים שבפנים`
  2. `` `3` הוא המקדם (המספר שכופל את `x`), ו-`3` כפול `2` הוא המספר החופשי ``
- steps:
  1. `כופלים את המספר שמחוץ לסוגריים בכל איבר בסוגריים`
  2. `` כופלים את המשתנה: `3 × x = 3x` ``
  3. `` כופלים את המספר: `3 × 2 = 6` ``
  4. `` הביטוי הפתוח: `3x + 6` ``

### `g8-expressions-m2` — `5(x + 4)`, answer 20
- hints:
  1. `כופלים את מה שמחוץ לסוגריים בכל אחד מהאיברים שבפנים`
  2. `` `5` הוא המקדם (המספר שכופל את `x`), ו-`5` כפול `4` הוא המספר החופשי ``
- steps:
  1. `כופלים את המספר שמחוץ לסוגריים בכל איבר בסוגריים`
  2. `` כופלים את המשתנה: `5 × x = 5x` ``
  3. `` כופלים את המספר: `5 × 4 = 20` ``
  4. `` הביטוי הפתוח: `5x + 20` ``

### `g8-expressions-m6` — `4(x + 3)`, answer 12
- hints:
  1. `כופלים את מה שמחוץ לסוגריים בכל אחד מהאיברים שבפנים`
  2. `` `4` הוא המקדם (המספר שכופל את `x`), ו-`4` כפול `3` הוא המספר החופשי ``
- steps:
  1. `כופלים את המספר שמחוץ לסוגריים בכל איבר בסוגריים`
  2. `` כופלים את המשתנה: `4 × x = 4x` ``
  3. `` כופלים את המספר: `4 × 3 = 12` ``
  4. `` הביטוי הפתוח: `4x + 12` ``

### `g8-expressions-m7` — `2(x + 7)`, answer 14
- hints:
  1. `כופלים את מה שמחוץ לסוגריים בכל אחד מהאיברים שבפנים`
  2. `` `2` הוא המקדם (המספר שכופל את `x`), ו-`2` כפול `7` הוא המספר החופשי ``
- steps:
  1. `כופלים את המספר שמחוץ לסוגריים בכל איבר בסוגריים`
  2. `` כופלים את המשתנה: `2 × x = 2x` ``
  3. `` כופלים את המספר: `2 × 7 = 14` ``
  4. `` הביטוי הפתוח: `2x + 14` ``

### `combineLikeTerms` (המחולל האדפטיבי, `src/data/adaptiveExpressions.ts`)
עבור `A${v} ${op} B${v}`, `answer`:

- Prompt: ללא שינוי — `` כנסו איברים: `${a}${v} ${op} ${b}${v}`. מה המקדם של `${v}`? ``
- hints:
  1. `אוספים יחד רק איברים עם אותו משתנה: המקדם הוא המספר שכופל את המשתנה` (שונה —
     נוספה הגדרת "מקדם" למשפט הקיים)
  2. `` `${a}` ${isAdd ? "ועוד" : "פחות"} `${b}` מאותו סוג `` (ללא שינוי)
- steps: ללא שינוי.
- analogy: ללא שינוי.

## Interaction Flow
זהה לזרימה הקיימת של כל שאלה באפליקציה: התלמיד/ה יכול/ה לבקש רמז לפני תשובה
(שני הרמזים החדשים/מעודכנים מוצגים בזה אחר זה, כמו היום); אחרי תשובה שגויה נחשף
פאנל ה-steps + analogy עם השלב הרביעי החדש (הביטוי הפתוח המלא). שום כפתור, מסך,
או מעבר חדש לא נוסף.

## Accessibility / RTL notes
כל ביטוי חשבוני חדש בטקסט (`3x + 6`, `3 × x = 3x`, וכו׳) עטוף בגרשיים אחוריים
בדיוק כמו כל שאר התוכן הקיים בקובץ, כך שהוא עובר דרך `segmented()` ומקבל
`unicode-bidi: isolate` בתוך המשפט העברי — אותו מנגנון קיים, שום דבר חדש. בדקתי
שאף אחד מהביטויים החדשים לא מוטמע כטקסט חופשי בתוך משפט עברי בלי גרשיים (המלכודת
שכבר קרתה בפרויקט עם `3(x + 2)`). ה-steps עצמם כבר משתמשים בשדה `math` הנפרד
(ולא בטקסט מעורב), כמו בכל שאר הקוד — נשמר כאן באותה צורה.

## Acceptance Criteria Check
- שלבי ההסבר מציגים ביטוי פתוח ומכונס מלא (`Ax + AB`) בכל שישה המקומות: yes —
  step 4 החדש בכל אחד.
- שני צעדי הכפל מוצגים בנפרד: yes — steps 2–3 החדשים.
- הגדרת "מקדם" ו"מספר חופשי" במשפט אחד: yes — hint 2 החדש בכל אחד מהמקומות
  שכוללים סוגריים; hint 1 המעודכן ב-`combineLikeTerms` מגדיר "מקדם" (אין שם
  "מספר חופשי" לשאלה, כי הדפוס לא שואל עליו).
- ארבע שאלות grade8.ts מתעדכנות לאותו מבנה ועקביות זו עם זו: yes — כל ארבע
  מקבלות אותו מבנה של 2 hints + 4 steps.
- `combineLikeTerms` מקבל הגדרת "מקדם": yes.
- אותה תשובה נכונה כמו היום: yes — אף prompt/answer/analogy לא השתנו.

## עדכון אחרי סבב ריוויו שני (2026-08-21)
המשתמש פסל את hint 2 הישן של `combineLikeTerms` בדף התצוגה המקדימה: "לא בטוח
שרמז 2 הוא טוב... תנסה למצוא משהו שיסביר אבל לא יפתור לגמרי". הניסוח הישן
(`` `${a}` ${isAdd ? "ועוד" : "פחות"} `${b}` מאותו סוג ``) היה למעשה המשוואה עצמה
במילים — לא נשאר כלום לחשב.

hint 2 החדש, במחולל `combineLikeTerms` **ובשש השאלות הכתובות המקבילות ב-`grade8.ts`**
(`e3`, `e5`, `e8`, `e10`, `m4`, `m9` — אותה מחרוזת ישנה בדיוק, אז אותו תיקון):

`` מסתכלים רק על המקדמים `${a}` ו-`${b}`, ו{מחברים/מחסרים} ביניהם — בלי לגעת ב-`${v}` ``

(לשלוש-איברים, `m4`/`m9`: `` מסתכלים רק על המקדמים `A`, `B` ו-`C`, ומחברים ביניהם — בלי לגעת ב-`v` ``)

מסמן **במפורש**: `` `${a}` ${isAdd?"ועוד":"פחות"} `${b}` `` (הצורה הישנה) עדיין
מופיע במחולל `openBrackets`'s hint 2 ובשלב 3 של ה-steps — שם זה תקין, כי זה
*אחרי* שכבר הוסבר שזה כפל הכנסת סוגריים, לא הצגה חוזרת של אותה שאלה.
`g8-expressions-h3`/`h8` (הדפוס "פתחו וכנסו") משתמשים בניסוח שונה לגמרי ולא נגעתי בהם.

## עדכון אחרי סבב ריוויו שלישי (2026-08-21)
המשתמש הצביע על ה-step הראשון (לא hint) של אותה שאלה: "איברים מאותו סוג זה לא
מסביר... תכתוב משהו כמו מחברים רק את המקדמים של אותו המשתנה (אצלנו Y)". תוקן
באותם שבעה מקומות (המחולל + `e3`/`e5`/`e8`/`e10`/`m4`/`m9`):

`` {מחברים/מחסרים} רק את המקדמים של אותו המשתנה (אצלנו `${v}`) ``

## Open Questions
None.
