# זום זמני בהחזקת מגע — התקרבות למחברת — Tests

**עדכון סבב ב׳ (2026-09-13):** כיוון הזום התהפך (התקרבות, לא התרחקות) —
הבדיקות עודכנו בהתאם (`toBeGreaterThan` במקום `toBeLessThan`, ניסוחים
"zoom-in"/"zoomed-in"). מבנה הבדיקות וכיסוי הקריטריונים לא השתנו.

## Coverage

מיפוי לקריטריוני הקבלה ב-`product-spec.md`:

| קריטריון קבלה | בדיקה |
|---|---|
| הפעלה רק בתחילת מגע, לפני תזוזה | `tests/e2e/notebook-hold-to-zoom.spec.ts` › "holding a single touch still from the very start zooms the view in temporarily" |
| אין שינוי תצוגה אם הכתיבה מתחילה מיד | › "starting to move immediately, without pausing first, never triggers the zoom — for the whole touch" |
| נמשך כל עוד המגע נמשך | › "the temporary zoom-in stays in effect while the same touch keeps moving (drawing) after it triggers" |
| חזרה מדויקת בהרמת האצבע | › "lifting the finger restores exactly the zoom and position from right before the touch started" |
| מה שנכתב נשאר במקום הנכון בדף | › "drawing that continues through a hold-zoom cycle is still recorded as page content" — **בעקיפין**, ראו "מה לא נבדק" |
| לא תלוי בכלי שנבחר | › "triggers the same way with the pan tool selected, not only with the pen" |
| לא תלוי בנעילת הדף | › "still triggers on a page that's already locked (answered and checked)" |
| לא מתנגש עם זום-שתי-אצבעות הקיים | **לא אוטומטי** — ראו "מה לא נבדק" |
| שאר הכלים נשארים כפי שהם | **לא נבדק כבדיקה נפרדת** — ראו "מה לא נבדק" |
| חל על שלושת התלמידים | **לא חזרה שלוש פעמים בכוונה** — ראו "מה לא נבדק" |

### Coverage — בורר העוצמה (סבב ה׳)

| קריטריון קבלה | בדיקה |
|---|---|
| שש אפשרויות, רשימה סגורה + ברירת המחדל | › "the strength setting offers exactly six options, with the validated strongest one chosen by default" (גם סופרת שש, גם מאמתת את ששת הכיתובים בסדר, גם שיש בדיוק אחד מסומן) |
| "כבוי" = המחווה לא קיימת | › "choosing \"כבוי\" means holding still changes nothing at all — not even momentarily" |
| הדרגות מונוטוניות (חלשה < חזקה) | › "a weaker level zooms in less than the strongest one" |
| הבחירה נשמרת בין סשנים | › "the choice survives a reload — both the marked option and the actual behaviour" |
| per-student | › "each student keeps their own strength" |
| השינוי נכנס לתוקף מיד | › "a change applies to the very next hold, with no reload in between" |
| ההגדרה לא בדרך של הילד/ה | **לא נבדק כבדיקה נפרדת** — ראו "מה לא נבדק" |

### Coverage — כפתור ההגדרות (סבב ו׳)

| קריטריון קבלה | בדיקה |
|---|---|
| האפשרויות לא מוצגות עד שפותחים | › "the practice-choice screen shows one settings button and none of the options" (גם `.hold-zoom-option`, גם `.hold-zoom-setting`, וגם `.read-aloud-setting` — אפס במסך עצמו) |
| כפתור אחד, שמבוגר מזהה | אותה בדיקה — בדיוק כפתור אחד בשם הנגיש `הגדרות`, ו-`toBeInViewport()` בשביל "בלי גלילה" |
| פתיחה מציגה את כל האפשרויות ואת הנבחרת | › "opening the settings shows both settings, titled, with the current choice marked" |
| סגירה בלי לבחור, וחזרה לאותו מקום | › "closing the settings without choosing returns to exactly the same screen — by \"סגירה\" and by tapping the backdrop" |
| גם הגדרת ההקראה עוברת לשם, בלי שינוי | › "the read-aloud setting moved into the settings and still works and saves there" |
| תנאי ההצגה של ההקראה נשמר (אין קול → לא מוצגת) | `tests/e2e/read-aloud-questions.spec.ts` › "a browser with no speech engine shows no setting and no button" — **הורחבה בסבב הזה** (ראו למטה) |
| הקריטריונים של סבב ה׳ נשארים בתוקף | שש הבדיקות של סבב ה׳, שכולן עוברות עכשיו **דרך** הכפתור |
| מקום אחד להגדרות | **לא נבדק כבדיקה נפרדת** — ראו "מה לא נבדק" |

**שלוש החלטות בכתיבת הבדיקות של הסבב הזה:**
- **סגירה נבדקת בשתי הדרכים בבדיקה אחת** (כפתור `סגירה` ולחיצה על הרקע), כי
  הקריטריון הוא אחד ("אפשר לסגור בלי לשנות, והמסך חוזר לאותו מקום") ושתי
  הדרכים הן שני מסלולים אליו. הלחיצה על הרקע נעשית ב-`position: {x: 5, y: 5}`
  — לחיצה במרכז הרקע הייתה נופלת על הכרטיס עצמו, כלומר בודקת את ההיפך.
- **"חזרה לאותו מקום" נמדדת בתוכן המסך, לא בהיעדר ניווט:** נשמרות הכותרת
  ורשימת שמות הנושאים לפני הפתיחה, ומושוות אחרי הסגירה. בדיקת URL הייתה
  חסרת משמעות באפליקציה בלי ניתוב.
- **הבדיקה של "אין קול במכשיר" הורחבה, לא הותאמה:** קודם היא בדקה
  `getByRole("switch")).toHaveCount(0)` על המסך — טענה שעכשיו הייתה עוברת
  מעצמה, כי כל ההגדרות מוסתרות עד שפותחים. היא עכשיו **פותחת** את ההגדרות,
  מאמתת שהמתג לא שם, **ו**שבורר הזום כן שם (כלומר שהכפתור לא פותח דיאלוג
  ריק). זו חיזוק של הבדיקה, לא החלשה כדי "לעבור".

**שני קבצי בדיקות אחרים נגעו — ניווט בלבד:** `read-aloud-questions.spec.ts`
(שש בדיקות שלחצו על המתג ישירות במסך; נוספו שני helpers,
`toggleReadAloud`/`readAloudState`, שעוטפים את הפתיחה והסגירה) ו-
`style-lessons.spec.ts` (בדיקה אחת שהדליקה הקראה כדי לראות רמקולים על
כרטיסי סגנון). **בשניהם הטענות עצמן לא נגעו** — רק הדרך אל הפקד. שניהם
נמצאו ע"י הריצה, לא ע"י ניחוש: `grep` על `.read-aloud-switch` פספס את
`read-aloud-questions.spec.ts` שניגש דרך `getByRole("switch")`.

**הערה על שבע הבדיקות מהסבבים הקודמים:** ברירת המחדל היא הדרגה החזקה
ביותר, כך שכולן בודקות בפועל את מצב ברירת המחדל — כלומר הן משמשות גם
כיסוי לכך שברירת המחדל מחוברת נכון, בלי בדיקה נוספת.

**שתי החלטות בכתיבת הבדיקות שכדאי להכיר:**
- **"כבוי" נבדק בדגימה חוזרת *בזמן* ההחזקה**, לא רק אחריה: הקריטריון אומר
  "גם לא שינוי רגעי שחוזר", ובדיקה שמסתכלת רק בסוף הייתה עוברת גם אם
  התצוגה קפצה וחזרה. הבדיקה דוגמת את ה-`transform` המחושב שמונה פעמים
  לאורך ההחזקה.
- **השוואות בין דרגות, לא מול מספרים קבועים** (`atWeakest < atStrongest`
  ולא `=== 91`): הסקאלה כבר כוונה פעמיים, והבטחת המוצר היא המונוטוניות —
  לא ערך מסוים. כך כיוונון עתידי לא ישבור את הסוויטה.

## מה לא נבדק, ולמה

- **זום-שתי-אצבעות תוך כדי hold-zoom (pinch שמשתלט):** דורש שני מגעים
  עצמאיים בו-זמנית. `page.mouse`/`page.touchscreen` ב-Playwright מניעים
  מגע פעיל אחד בלבד — זה לא ייחודי לפיצ'ר הזה: לזום-שתי-אצבעות הקיים
  באפליקציה **אין ולא הייתה** אף פעם בדיקת e2e, מהסיבה הזו בדיוק (נבדק:
  `grep` על "pinch"/"PointerEvent"/"touchscreen" בכל `tests/e2e/` לא מוצא
  כלום). **צריך אימות ידני:** מחזיקים אצבע אחת עד שהתצוגה מתקרבת, ואז
  מוסיפים אצבע שנייה ומצבטים (pinch) — ה-pinch צריך להשתלט בצורה חלקה,
  ובהרמת שתי האצבעות **אסור** שהתצוגה "תיזרוק" חזרה למצב שהיה שמור מלפני
  ה-hold-zoom.
- **שאר הכלים (הזזה/pinch) נשארים כפי שהם:** זו טענת "אין רגרסיה" על
  התנהגות קיימת, לא התנהגות חדשה. הכיסוי היא הסוויטה הקיימת (בעיקר
  `notebook-usability-fixes.spec.ts`, שבודקת זום פתיחה, מסך-מלא, ו"הצג
  את כל הדף") שממשיכה לעבור בלי שינוי, לא בדיקה נפרדת חדשה.
- **חל על שלושת התלמידים:** המחווה חיה כולה ב-`PracticeNotebook.tsx` בלי
  שום ענף תלוי-תלמיד/כיתה — חזרה על אותה בדיקה שלוש פעמים הייתה בודקת את
  אותו נתיב קוד שלוש פעמים, לא כיסוי אמיתי נוסף.
- **"מה שנכתב נשאר במקום הנכון בדף" — נבדק בעקיפין, לא פיקסל-מדויק:**
  הבדיקה מאמתת שציור שממשיך תוך כדי מחזור hold-zoom עדיין נרשם כתוכן
  (מפעיל את "שלח למורה"), לא שהמיקום המדויק בפיקסלים נכון — לרכיב הזה אין
  שכבת unit test שתאפשר לבדוק מתמטיקת קואורדינטות ישירות, ובדיקת פיקסלים
  על קנבס חי מ-e2e הייתה מוסיפה שביריריות משל עצמה. הסיכון בפועל נמוך: זה
  שינוי תצוגה בלבד (כמו זום-שתי-אצבעות הקיים, שגם לו אין בדיקה ישירה
  לקואורדינטות), לא שינוי לאיך שהכתיבה נשמרת.
- **"ההגדרה לא בדרך של הילד/ה" (סבב ה׳) — קריטריון על מה ש*לא* קיים,
  ולכן אין לו בדיקה חיובית טובה.** מה שכן נבדק בעקיפין: כל שבע בדיקות
  המחווה נכנסות לתרגול דרך המסלול הרגיל בלי לגעת בבורר אף פעם — כלומר אם
  הבורר היה הופך לשלב חובה בדרך, כולן היו נשברות. מה שלא נבדק: שאי אפשר
  להגיע אליו בטעות ממשטח הכתיבה — הוא פשוט לא קיים במסך התרגול, ובדיקה
  ש"אלמנט לא קיים במסך" היא בדיוק סוג הבדיקה שעוברת גם כשהיא מחפשת סלקטור
  שהשתנה.
- **"מקום אחד להגדרות" (סבב ו׳) — קריטריון על מה ש*אין* במסכים האחרים.**
  בדיקה שעוברת על כל מסך באפליקציה ומאמתת שאין בו כפתור `הגדרות` הייתה
  ארוכה, שברירית, ובעיקר הייתה נכשלת ברגע שנוסיף בכוונה נקודת כניסה שנייה —
  כלומר מודדת את הקוד ולא את ההבטחה. מה שכן נבדק בעקיפין: כל שאר הבדיקות
  בסוויטה שנכנסות לתרגול לא רואות שום כפתור כזה בדרך (הן היו נשברות אם
  ההגדרות היו הופכות לשלב).
- **הערכים המספריים עצמם (180ms/30%/120ms/4px) אינם חלק מהבדיקות** — נבדקת
  ההתנהגות האיכותית (יש שינוי / אין שינוי / חוזר בדיוק), לא מספר קבוע, כך
  שכיוונון עתידי של הערכים (ציפייה מפורשת ב-architecture.md) לא ישבור את
  הסוויטה הזו.

**2026-09-25, סבב ה׳ (בורר העוצמה), ריצה בודדת ונקייה:** נוספו שש בדיקות
לקריטריונים החדשים; הקובץ הזה מחזיק עכשיו **13** בדיקות, כולן עוברות.
הסוויטה המלאה — **382/382**, כולל הבדיקה הקיימת-מראש שנהגה להיכשל
(`grade8-angles-congruence.spec.ts`) שעברה גם הפעם, וכולל הבדיקה שמשווה את
מספר הגרסה במסך הראשון ל-`package.json` (הגרסה הועלתה בסבב הזה ל-`1.32.0`).

**שני כשלים בדרך, שניהם בבדיקות ולא באפליקציה, ותוקנו:** ההנחה שאחרי
`reload` או יציאה מתרגול חוזרים למסך הנושאים — בפועל חוזרים למסך
ה*סגנונות*, ו"← חזרה" עולה רמה אחת בכל פעם. נוסף helper שמטפס עד ששורת
ההגדרה נראית, במקום מספר קבוע של לחיצות. ההשוואות עצמן לא נוגעו — רק
הניווט אליהן.

## How to run
```
npm run test:e2e
```
לבדיקה הזו בלבד: `npx playwright test tests/e2e/notebook-hold-to-zoom.spec.ts`

## Status
**2026-09-12, סבב א׳ (כיוון שהתברר כשגוי), ריצה בודדת ונקייה:** שבע הבדיקות
עברו. הסוויטה המלאה (373 בדיקות) — 372 עוברות, כשל אחד
(`grade8-angles-congruence.spec.ts`) אומת כ-flake קיים-מראש ולא-קשור, נכשל
גם על `origin/main` נקי בלי השינוי הזה בכלל (שתי ריצות מתוך שלוש).

**2026-09-13, סבב ב׳ (כיוון תוקן להתקרבות), ריצה בודדת ונקייה:** שבע
הבדיקות עודכנו לכיוון הנכון (`toBeGreaterThan`) ועוברות. הסוויטה המלאה —
שוב **372/373**, כשל יחיד באותה בדיקה קיימת-מראש (`grade8-angles-congruence.spec.ts`
"the triangle-angle-sum step...") — לא הורץ אימות חדש מול `origin/main`
כי זה כבר אומת בסבב א׳ ולא נגעתי בשום דבר שקשור לאותה בדיקה. לא
נזרק/הוחלש כדי "לעבור" — פשוט מדווח כמו שהוא.

**2026-09-13, סבב ג׳ (תיקון באג ממכשיר מגע — CSS + onContextMenu), ריצה
בודדת ונקייה:** **373/373** — כולל הבדיקה הקיימת-מראש שהייתה כשלה בשתי
הריצות הקודמות, עברה הפעם (עקבי עם flake, לא עם תיקון — לא נגעתי בה).
התיקון עצמו (דיכוי callout/הדגשת-טקסט/תפריט-הקשר על מגע-ממושך) **לא נבדק
ולא ניתן להיבדק** ע"י הסוויטה — תלוי בזיהוי-מחוות אמיתי של דפדפן מגע, ש-
Playwright לא מדמה. אימות בפועל דורש שהמשתמש ינסה שוב על אותו מכשיר מגע
שדיווח ממנו.

**2026-09-26, סבב ו׳ (כפתור ההגדרות), ריצה בודדת ונקייה:** נוספו **ארבע**
בדיקות לקריטריוני סבב ו׳ (הקובץ מחזיק עכשיו **17**), ושש הבדיקות מסבב ה׳
עברו לגשת לבורר דרך הכפתור (נוספו helpers `settingsButton`, `openSettings`,
`closeSettings`, `chooseStrength`; `backToTopics` מטפס עכשיו עד שהכפתור נראה
ולא עד שהבורר נראה). כל 17 עוברות. הסוויטה המלאה — **385/386**: הכשל היחיד
הוא אותה בדיקה קיימת-מראש (`grade8-angles-congruence.spec.ts`, "the
triangle-angle-sum step...") שאומתה בסבב א׳ כ-flake לא-קשור (נכשלת גם על
`origin/main` נקי); היא לא נגעה ולא הוחלשה, ומדווחת כמו שהיא. **אין צורך
בהעלאת גרסה בסבב הזה**, ולכן הבדיקה שמשווה את מספר הגרסה במסך הראשון
ל-`package.json` עברה בלי שינוי.
