# ציורים לשאר הנושאים — Architecture

## Overview
גל ראשון: שתי הצורות של מיקה. שני מודולי נתונים חדשים בתבנית של `fractionDiagram.ts`
— טהורים, `import type` בלבד, נצרכים רק ממסך התרגול — ושני רכיבי SVG בתבנית של
`FractionCircle.tsx`. חמש הצורות הנותרות אינן כאן.

## Affected Files / Components

| קובץ | מה משתנה |
|---|---|
| `src/data/numberLine.ts` | **חדש.** `numberLine(question): NumberLine \| null` — חילוץ, אימות מול `answer`, חלון הסימונים, והכיתוב. |
| `src/data/tenFrame.ts` | **חדש.** `tenFrame(question): TenFrame \| null` — אותו דבר לקופסאות העשר. |
| `src/components/NumberLine.tsx` | **חדש.** SVG בלבד: ציר, סימונים, `●`, `★`, חץ. |
| `src/components/TenFrame.tsx` | **חדש.** SVG בלבד: רשת `2×5` והבודדות לצידה. |
| `src/components/Practice.tsx` | שני `figure` נוספים בקופסת ההסבר, ושני כיתובים לרצף ההקראה. |
| `src/App.css` | `.number-line`, `.ten-frame` וסגנונות השורות. |
| `tests/e2e/content.spec.ts` | שני כללים סופרים (`30` ו-`20`) ושני כללי אי-סתירה. |

**מודול נפרד לכל צורה, לא מודול משותף.** שלוש הצורות הקיימות בריפו
(`fractionDiagram`, `verticalSum`, ועכשיו אלה) חולקות *חוזה* — `(question) → shape |
null`, אימות מול `answer`, כיתוב אחד — אבל **אין להן קוד משותף**: החילוץ שונה לגמרי,
והגיאומטריה שונה לגמרי. הפשטה לשש צורות שטרם נכתבו תתאים למה שדמיינו ולא למה שיתגלה.
החוזה החוזר הוא מה ששומר על אחידות, לא מחלקת בסיס.

## Data / State Changes

```ts
export interface NumberLine {
  /** Every tick to draw, ascending. At least five. */
  ticks: number[];
  /** Marked ● — the number the question is about. */
  from: number;
  /** Marked ★ — the answer. */
  to: number;
  caption: string;
}

export interface TenFrame {
  /** How many full tens boxes — always 1 in this data, but read, not assumed. */
  tens: number;
  units: number;
  /** Which side the question asks about, and so which side is filled. */
  highlight: "tens" | "units" | "both";
  caption: string;
}
```

אין state חדש ב-`Practice`. כמו `diagram` ו-`vertical`, שניהם נגזרים מ-`question`
בכל רינדור.

## Technical Approach

### `numberLine.ts` — חמש צורות שאלה, לא ארבע
סקירת הנתונים העלתה צורה חמישית שלא הופיעה בתדריך: **`מה קטן יותר`** (`h5`–`h8`).
כל `30` השאלות מתחלקות כך, והזיהוי הוא לפי הניסוח בלבד:

| צורה | שאלות | `●` | `★` |
|---|---|---|---|
| `בא אחרי N` | 8 | `N` | `N + 1` |
| `בא לפני N` | 7 | `N` | `N − 1` |
| `מה גדול יותר, A או B` | 7 | הקטן | הגדול |
| `מה קטן יותר, A או B` | 4 | הגדול | הקטן |
| `נמצא בין A ל-B` | 4 | `A` **ו**-`B` | `A + 1` |

`מה גדול/קטן יותר` הן היחידות שבהן `●` אינו "המספר שבשאלה" אלא **המספר שלא נבחר** —
שתי האפשרויות על הציר, והתשובה מסומנת בכוכב. זה מה שהופך את הציר להסבר ולא לקישוט:
רואים למה אחד רחוק יותר.

`נמצא בין` מסמנת **שני** קצוות, ולכן `from` לבדו אינו מספיק. הפתרון: `ticks` נושא את
כל הסימונים, ומי שמסומן נקבע ב-`from`/`to`; לצורת "בין" נוסף `from` שני דרך אותו
מנגנון — **החלטה: `from` הוא `number[]`, לא `number`.** תא אחד בארבע הצורות הראשונות,
שניים בחמישית.

**האימות:** מחשבים את התשובה מהצורה ומשווים ל-`question.answer`.
`בא אחרי 12` → `13` ✓. `מה גדול יותר, 13 או 18` → `Math.max` ✓.
נכשל → `null`, ואין ציור ואין כיתוב.

**חלון הסימונים:** `lo = min(כל המסומנים) − 2`, `hi = max(...) + 2`, נחתך ל-`0..20`,
ומורחב אם יצאו פחות מ-`5`. `מה גדול יותר, 4 או 8` נותן `2..10` — תשעה סימונים, וזה
בסדר; `מה גדול יותר, 13 או 18` נותן `11..20`. **תקרה של `11` סימונים**, ומעליה
מציגים רק את הקצוות והמסומנים — אין כזה מקרה בנתונים היום, והתקרה קיימת כדי
שהוספת שאלה לא תשבור את הפריסה בשקט.

### `tenFrame.ts` — ארבע צורות, `20` מתוך `30`
| צורה | שאלות | `highlight` |
|---|---|---|
| `כמה עשרות במספר N` | 5 | `tens` |
| `כמה יחידות במספר N` | 5 | `units` |
| `עשרת אחת ועוד N יחידות` | 5 | `both` |
| `10 + N` | 5 | `both` |

**האימות:** `tens × 10 + units` חייב להיות המספר שהשאלה עוסקת בו, ובנוסף התשובה
עצמה חייבת להתאים לצורה — `כמה עשרות` מול `tens`, `כמה יחידות` מול `units`,
ושתי האחרונות מול המספר השלם. שתי בדיקות, לא אחת: הראשונה שהציור מתאר את המספר
הנכון, השנייה שהוא עונה על מה שנשאל.

**עשר השאלות שנשארות בחוץ** — `h1`–`h5` (הפרש בין ספרות) ו-`h6`–`h10` (השלמה
ל-`20`) — מחזירות `null`. קופסה אחת אינה מתארת אף אחת מהן, וזה מכוון ורשום בספק.

### רינדור — SVG לשתיהן, `280` רוחב
העיגול הוא `180` כי אין לו ציר ארוך; לשתי הצורות האלה יש, ו-`design.md` קובע `280`
עם `max-width: 100%`. שתיהן גיאומטריה — קו עם סימונים, ורשת עיגולים — ולכן SVG,
באותה תבנית בדיוק של `FractionCircle.tsx`: קבועי גודל בראש הקובץ, `role="img"`,
`aria-label` שהוא הכיתוב בלי גרשיים אחוריים.

**הכוכב** מצויר כ-`path` של עשר נקודות סביב מרכז הסימון — לא תו טקסט, שהרנדור שלו
משתנה בין מכשירים.

### הקראה
`Practice.tsx` מוסיף את שני הכיתובים לאותו מערך, בסדר התצוגה:
`[method?, diagramCaption?, verticalCaption?, numberLineCaption?, tenFrameCaption?, ...steps]`
— בפועל לעולם לא יופיעו שניים יחד, כי הם חיים בנושאים שונים. מיקה אינה קוראת;
הכיתוב הוא הערוץ שלה, ולכן הוא משפט מלא ולא רשימת מספרים.

## Edge Cases
- **`בא לפני 10`** (`h9`) → חלון `8..12`; `0` אינו חסם כאן. **`בא אחרי 19`** (`m4`)
  → `17..20`, נחתך בתקרה — ולכן יוצאים ארבעה סימונים בלבד וההרחבה ל-`5` מוסיפה
  את `16`. זה המקרה שמחייב את ההרחבה להיות **דו-כיוונית** ולא רק ימינה.
- **`מה גדול יותר, 20 או 14`** (`m10`) → `12..20`, תשעה סימונים. הרוחב מתחלק
  שווה בשווה, וסימון דו-ספרתי ב-`280` פיקסלים עם תשעה תאים הוא `31` פיקסלים לתא —
  נכנס.
- **`עשרת אחת ועוד 4 יחידות`** — המספר שהשאלה עוסקת בו הוא **התשובה** (`14`),
  לא מספר בניסוח. החילוץ חייב לקרוא `1` ו-`4` מהניסוח ולבנות `14`, ואז לאמת מולה.
- **`10 + 6`** — הצורה היחידה שהיא ביטוי חשוף, ולכן גם `verticalSum` יכול לתפוס
  אותה. שתי הצורות יופיעו יחד על אותה שאלה, וזה תקין: המאונך מראה את החיבור,
  הקופסה את מבנה המספר. **סדר התצוגה קבוע** — קופסה ואז מאונך.
- **כיווניות:** `direction: ltr` על שני ה-`<svg>` ועל כל `<text>` בתוכם. הציר עולה
  משמאל לימין בתוך העמוד העברי — הוכרע בדיון.

## Risks / Tradeoffs
- **`from: number[]`** נראה מיותר בארבע מתוך חמש הצורות, אבל צורת "בין" דורשת אותו
  והחלופה — שדה `from2` אופציונלי — הייתה מוסיפה ענף לכל צרכן.
- **הכפילות בין `numberLine.ts` ל-`tenFrame.ts`** מכוונת. הן חולקות חוזה ולא קוד,
  וזה מה שיאפשר לחמש הצורות הבאות להיכתב בלי לשבור אף אחת מהקיימות.
- **מה נשבר:** בדיקות שסופרות `.explanation-step` אינן מושפעות. בדיקות שפותחות את
  המסך של מיקה בנושא "מספרים עד 20" — `mistake-diagnosis.spec.ts` עובד שם — צריכות
  לעבור כמו שהן, כי הציור מתווסף ואינו מחליף. QA יאמת.

## Open Questions

None.

## Implementation Notes

נבנה כמתוכנן: `numberLine.ts`, `tenFrame.ts`, `NumberLine.tsx`, `TenFrame.tsx`,
החיווט ב-`Practice.tsx` והסגנון ב-`App.css`. חמש הצורות הנותרות לא נגעו.

**שתי סטיות, שתיהן התגלו מול המסך ולא בתכנון:**

1. **פריסת הקופסה נבנתה מחדש.** הבודדות צוירו תחילה גדולות יותר מהמנוקדות שבקופסה
   ובשורה אחת — ועל `10 + 6` השישית חרגה מ-`280` הפיקסלים. עכשיו **הבודדות יושבות
   על אותה רשת בדיוק ובאותו גודל** כמו אלה שבקופסה, חמש בשורה. זה גם נכון יותר
   פדגוגית: הן אותו סוג של דבר, רק לא בתוך הקופסה.

2. **הסף ירד מ-`99` ל-`19`.** התכנון קרא את מספר הקופסאות מהנתונים במקום להניח
   אחת — נכון — אבל שתי קופסאות אינן נכנסות ל-`280` פיקסלים לצד הבודדות. הצורה
   מסרבת במקום לגלוש. כל שאלות הנושא הן `10`–`19`, ולכן זה לא מוריד ולו שאלה אחת.

**אימות ידני מול האפליקציה**, ששת המקרים מהתדריך:
`בא אחרי 12` (חלון `10`–`15`, ● ו-★), `מה גדול יותר, 4 או 8` (חלון `2`–`10`,
● על `4` שהוא **לא** התשובה), `נמצא בין 6 ל-8` (**שני** ●), `כמה יחידות במספר 13`
(קופסה, הבודדות מלאות), `10 + 6` (קופסה ואז מאונך, בסדר הזה), ו-`בכמה גדולה ספרת
היחידות` — בלי ציור, כמתוכנן.

`130 passed` בסוויטה הקיימת; הכללים הסופרים הם עבודת QA.

---

## גל התיקונים (2026-08-15)

הריוויו על גל א׳ החזיר שש הערות. חמש נופלות על הקוד; **ארבע מהן משנות מחרוזות
ולא מבנה**, ורק אחת מוסיפה מודול. הפירוט הקובע ב-`product-spec.md` ("גל התיקונים")
וב-`design.md` (סעיפים `1`, `2ב`, וטבלאות ה-Copy).

### Affected Files / Components — התוספת

| קובץ | מה משתנה |
|---|---|
| `src/data/numberLine.ts` | ריצת ספירה, שורת הכלל, וסדר החישוב. `caption` הופך ל-`string[]`. |
| `src/data/twentyStrip.ts` | **חדש.** `twentyStrip(question): TwentyStrip \| null` לחמש שאלות ההשלמה ל-`20`. |
| `src/components/TwentyStrip.tsx` | **חדש.** SVG בלבד: `2×10` עיגולים, מלאים מול ריקים. |
| `src/components/NumberLine.tsx` | ללא שינוי מהותי — `from` כבר מערך, וריצה היא עוד נקודות. |
| `src/components/Practice.tsx` | הרצועה נכנסת לסדר התצוגה ולרצף ההקראה; הכיתוב הרב-שורתי של הציר. |
| `src/data/method.ts` | משפט אחד מנוסח מחדש (עשרות ויחידות). |
| `src/data/grade1.ts` | `12` מחרוזות ב-`7` שאלות — **שינוי תוכן, לא ריפקטור**. |
| `src/App.css` | `.twenty-strip` והכיתוב הדו-שורתי. |
| `tests/e2e/*` | עבודת QA — ראו "מה נשבר" למטה. |

### 1. הכיתוב של הציר: `caption: string[]`

הכיתוב שירת עד היום שלושה צרכנים מאותה מחרוזת — `figcaption`, `aria-label`,
והקראה. עכשיו הוא שתי שורות, ושלושת הצרכנים רוצים אותן אחרת: מוצג כשתי שורות,
מוקרא כשני חלקים עם הפסקה, ומתואר לקורא מסך כתיאור **אחד**.

**ההכרעה: `caption: string[]` על `NumberLine` בלבד.**

- `figcaption` — שורה לכל איבר.
- `aria-label` — `caption.join(" ")` בלי גרשיים אחוריים. תיאור אחד, כי הציור אחד.
- הקראה — `speechParts(line.caption)`, שכבר מקבל מערך ומייצר חלק לכל איבר. זה
  בדיוק המנגנון שמפצל את ההסבר לשלבים, ולכן ההפסקה מגיעה בחינם.

**שלוש הצורות הקיימות נשארות `caption: string` ואינן מומרות.** המרתן הייתה עוטפת
מחרוזת במערך בשלושה מקומות ובכל אתרי הקריאה, בלי שאף אחת מהן צריכה שורה שנייה.
החוזה המשותף מנוסח מחדש כ**"הכיתוב הוא מה שקורא מסך מקבל ומה שמוקרא; צורה
שההסבר שלה דורש הפסקה מכריזה עליו כשורות"** — והוא נשמר בשתי הצורות.
(`design.md` כבר סימן את **היחס** כשתי-שורות, ולכן היא תגיע לכאן כשתיבנה.)

### 2. סדר החישוב — הנוסח נגזר מה-`ticks`, לא מהערכה

זו הכרעה מבנית ולא סגנון. שורת הכלל נבדלת בשני נוסחים לפי **אם ה-`0` באמת בתוך
החלון**, והחלון מחושב היום **אחרי** שנקבע הכיתוב:

```
היום:  marks(prompt) → { from, to, caption }  →  window()   ← הכיתוב כבר נכתב
צריך:  marks(prompt) → { from, to, sentence } →  window() → ticks → rule → caption
```

**`marks()` מפסיק להחזיר כיתוב ומחזיר את משפט השאלה בלבד.** שורת הכלל נבחרת
מ-`ticks.includes(0)` על ה-`ticks` הסופיים, ומצטרפת לפניו. כל דרך אחרת — לחשב
את החלון פעמיים, או להסיק את נראות ה-`0` מהמסומנים — מחזירה בדיוק את הפער
שהמשתמש הצביע עליו: טקסט שמתאר משהו שאינו על המסך.

### 3. ריצת הספירה

`from` הוא כבר `number[]` (בגלל "נמצא בין"), ולכן **אין שינוי טיפוס** — ריצה היא
עוד איברים באותו מערך, והרכיב מצייר `●` לכל אחד בלי לדעת מה הם.

הריצה נבנית מהמספר שבשאלה אחורה בכיוון ההפוך לתשובה, **עד שלושה איברים, נחתכת
ל-`0`–`20`**:

| שאלה | `from` | `to` |
|---|---|---|
| `בא אחרי 12` | `[10, 11, 12]` | `13` |
| `בא לפני 14` | `[16, 15, 14]` | `13` |
| `בא לפני 20` | `[20]` | `19` |
| `נמצא בין 6 ל-8` | `[4, 5, 6, 8]` | `7` |
| `מה גדול יותר, 13 או 18` | `[13]` | `18` |

**שתי ההשוואות אינן מקבלות ריצה** — הן אינן שאלות ספירה, ושתי האפשרויות הן בדיוק
מה שצריך להיות מסומן. זה גם מה ששומר את הבדיקה הקיימת `a comparison marks the
number that was NOT the answer` על `.nl-from` בכמות `1`.

### 4. `twentyStrip.ts` — מודול נפרד, לפי הכלל שכבר נקבע

`architecture.md` קבע למעלה: **"מודול נפרד לכל צורה, לא מודול משותף... הן חולקות
חוזה ולא קוד"**. הכלל תקף כאן ובמיוחד כאן.

`tenFrame` ו-`twentyStrip` חולקות **נושא** — אותן `30` שאלות — ולא שום דבר אחר:
- `tenFrame` מחלצת מספר דו-ספרתי ושואלת על **איזה צד**;
- `twentyStrip` מחלצת "כמה להוסיף ל-`N` כדי להגיע ל-`20`" ומחשבת `20 − N`.

איחודן היה מכניס שני מנתחים שאין ביניהם דבר מאחורי נקודת כניסה אחת, והצרכן היה
מסתעף בכל מקרה. **ובנוסף — והחשוב יותר — הן חייבות להיות זרות זו לזו כדי שיהיה
אפשר לבדוק שהן זרות:** `tenFrame` מחזירה `null` על `h6`–`h10`, `twentyStrip`
מחזירה `null` על כל השאר, ואף שאלה אינה מפיקה את שתיהן. **QA מוזמן לאכוף את זה
ככלל.**

```ts
export interface TwentyStrip {
  /** How many of the twenty are filled — the number the question starts from. */
  filled: number;
  /** The rest. This is the answer, and that is the whole check. */
  empty: number;
  caption: string;
}
```

**האימות:** `filled + empty === 20` **וגם** `empty === question.answer`. שני
מקורות בלתי תלויים, כמו כל צורה.

### 5. סדר התצוגה וההקראה

הרצועה נכנסת מיד אחרי הקופסה, שהיא הצורה הקרובה לה בנושא. הסדר המלא:

```
משפט שיטה → עיגול שברים → קופסת עשר → רצועת עשרים → מאונך → ציר → שלבים → דימוי
```

בפועל קופסה ורצועה לעולם לא יופיעו יחד — הן זרות לפי סעיף `4` — והסדר קבוע כדי
שלא תהיה שאלה אם זה ישתנה.

רצף ההקראה ב-`Practice.tsx` מקבל את הרצועה באותו מקום, ואת הציר כ-`speechParts(line.caption)`
במקום `speechParts([line.caption])`.

### 6. שינוי התוכן — אילו קבצים בדיוק

**זה לא ריפקטור.** ספירה בפועל: `רחוק יותר` מופיע **`12` פעמים ב-`7` שאלות**, כולן
ב-`src/data/grade1.ts` בנושא "מספרים עד `20`" (`e7`–`e10`, `m8`–`m10`), ואפס פעמים
ב-`grade6.ts` ו-`grade8.ts`. הנוסחים המחליפים בטבלה ב-`design.md`.

`src/data/method.ts` — משפט אחד, של "עשרות ויחידות". הוא רץ ממילא על כל `30`
שאלות הנושא, ולכן חמש שאלות "בכמה גדולה ספרת היחידות" — שאין להן ציור ולא יהיה
להן — מקבלות דרכו את מה שהריוויו ביקש.

### מה נשבר בבדיקות הקיימות

**לא לתקן כאן — זו עבודת QA.** רשום כדי שלא יתגלה כהפתעה:

| בדיקה | למה |
|---|---|
| `a between-question marks both ends` | מצפה ל-`.nl-from` בכמות `2`; אחרי הריצה זה `4` (`4`,`5`,`6` ועוד הקצה `8`). הכוונה של הבדיקה — שהקצה השני מסומן — **נשארת נכונה** וצריכה ניסוח מחדש, לא ביטול. |
| `the captions are sentences, not lists of numbers` | מצפה ל-`על הציר` בכיתוב. הנוסח החדש פותח ב-`זהו ציר המספרים`. |
| `each picture's caption is what a screen reader is told` | משווה `figcaption.innerText` ל-`aria-label`. שתי שורות מול מחרוזת מחוברת ברווח — ההשוואה צריכה נרמול רווחים, אחרת היא נכשלת על שינוי אמיתי ותקין. |
| `content.spec.ts` › `exactly twenty place-value questions draw their box` | **אינה משתנה.** `tenFrame` נשארת על `20`; ה-`25` בספק הוא `20` קופסאות ועוד `5` רצועות, ולכן נדרש **כלל שלישי** שסופר `5`, ולא עדכון של הקיים. |

שאר הבדיקות של הציר עוברות כמו שהן: `בא אחרי 12` נותנת חלון `8`–`15` (שמונה
סימונים, מכיל `12` ו-`13`), ו-`בא אחרי 19` נותנת `15`–`20` (שישה, מקסימום `20`).

### Edge Cases — התוספת
- **`בא לפני 20`, פעמיים בנתונים.** ריצה אחורה היא `22`,`21`,`20` — מחוץ לטווח.
  נחתכת לסימון אחד. זה מקרה אמיתי בנתונים ולא היפותטי, והוא הסיבה שהריצה
  **מתקצרת** במקום שהחלון יתרחב.
- **`בא אחרי 3`** — ריצה `1`,`2`,`3`, בגבול התחתון ונכנסת.
- **`נמצא בין 17 ל-19`** — ריצה `15`,`16`,`17`, כוכב `18`, קצה `19`. חלון
  `13`–`20`, שמונה סימונים.
- **שני נוסחי שורת הכלל שניהם מופעלים בנתונים האמיתיים**, ולכן שניהם ניתנים
  לבדיקה: `בא אחרי 3` נותן חלון `0`–`6` וה-`0` **נראה**; `בא לפני 4` נותן `1`–`8`
  וה-`0` **אינו נראה**, במרחק סימון אחד בלבד מהקצה. הנוסח נגזר מ-`ticks` ולכן זה
  נכון אוטומטית ואינו דורש טבלה ידנית — וזו בדיוק הסיבה לסעיף `2`.
- **הרצועה תמיד `20` עיגולים בדיוק**, גם כשהתשובה `1` (`h8`). אין מקרה של גלישה.

### Risks / Tradeoffs — התוספת
- **`caption` בשני טיפוסים בין מודולים אחיים** הוא חוסר אחידות אמיתי, ונבחר במודע
  על פני המרה של שלוש צורות עובדות שאף אחת מהן אינה צריכה שורה שנייה.
- **שורת הכלל חוזרת על עצמה בכל שאלה בנושא** — `30` פעמים. זו כוונת העיצוב ולא
  תופעת לוואי; אצל מי שאינה קוראת, חזרה היא איך כלל נלמד.
- **הריצה קצובה בשלושה** ולא בארבעה או בכל הדרך ל-`0`: שלושה מספרים הם מה שהמשתמש
  כתב, והם גם מה שנכנס בחלון בלי לדחוף את התקרה של `11` סימונים.

### Implementation Notes — גל התיקונים

נבנה כמתוכנן: `numberLine.ts` (ריצה, `caption: string[]`, היפוך סדר החישוב),
`twentyStrip.ts` + `TwentyStrip.tsx`, החיווט ב-`Practice.tsx`, משפט אחד ב-`method.ts`,
המחרוזות ב-`grade1.ts`, והסגנון ב-`App.css`. גרסה `1.13.0`.

**שלוש סטיות, וכולן קטנות מהתכנון:**

1. **הספירה של "רחוק יותר" הייתה `12` והאמת היא `16`.** החיפוש שעליו נשען התכנון
   חיפש את המחרוזת `רחוק יותר`, ופספס ארבעה שלבים שכתובים `ככל שסופרים **פחות
   רחוק**, המספר קטן יותר` — ארבע שאלות `מה קטן יותר`, כלומר בדיוק המקרה המשלים
   לזה שהמשתמש הצביע עליו. הן תוקנו ל-`ככל שזזים שמאלה על הציר, המספר קטן יותר`.

   זה בדיוק מה שהספק התכוון אליו כשקבע כלל ולא תיקון נקודתי, וזה גם אזהרה ל-QA:
   **כלל שמחפש מחרוזת יפספס את אותה שגיאה בסדר מילים אחר.**

2. **`ואחריו` מול `ואחריהם`.** הריצה של `בא לפני 20` מתכווצת לאיבר אחד, ותבנית
   הקופי הקבועה הייתה מייצרת `20, ואחריהם בא 19` — עברית שגויה מול ילדה שלומדת
   לקרוא. הכינוי מותאם לאורך הריצה. זה פרט דקדוקי, לא שינוי עיצוב.

3. **`רחוק` נשארת בשלוש שאלות בכיתה ח׳** — `כמה רגלו רחוקה **מהקיר**`. הכלל הוא
   "רחוק בלי לומר ממה", ולשלוש האלה יש נקודת מוצא מפורשת. **כלל QA שיאסור את
   המילה עצמה ייפול עליהן בטעות.**

**אימות מול האפליקציה**, שבעה מסכים — חמשת המקרים מהתדריך ועוד שניים:

| מקרה | מה נראה |
|---|---|
| `בא לפני 14` | ● `16`,`15`,`14` · ★ `13` · חלון `11`–`18` |
| `בא לפני 20` | ריצה מתכווצת לסימון אחד, ו-`ואחריו בא` |
| `מה גדול יותר, 13 או 18` | חלון `11`–`20`, הנוסח **"מחוץ לתמונה"**, והשלב שמתחת אומר `ימינה` ולא `רחוק` |
| `בא אחרי 3` | חלון `0`–`6`, הנוסח **"כאן משמאל"**, וה-`0` באמת על הקו |
| `נמצא בין 6 ל-8` | ארבעה ●: `4`,`5`,`6` ועוד `8` · ★ `7` |
| `כמה להוסיף ל-13` | שורה ראשונה מלאה ועוד `3` — העשרת נראית בעין |
| `במספר 16, בכמה גדולה` | בלי ציור, ומשפט הספרות בשמן פותח את ההסבר |

**ספירה מדודה על הנתונים:** `30` צירים, `20` קופסאות, `5` רצועות, **`0` חפיפה**
בין קופסה לרצועה. `build` ו-`lint` נקיים. הבדיקות הן עבודת QA, ושלוש מהן צפויות
להיכשל כרשום למעלה.

---

## גל ב׳ — חמש הצורות הנותרות (2026-08-17)

חמש הצורות שלא נכנסו לגל א׳: שטח והיקף (`#3`), פיתגורס (`#4`), אחוזים (`#5`), יחס
(`#6`), פונקציה קווית (`#7`) — לפי המספור ב-`product-spec.md`. אותו חוזה בדיוק
שנקבע למעלה: מודול נתונים טהור לכל צורה, `(question) → shape | null`, אימות מול
`question.answer`, ורכיב SVG ברוחב `280`. **אין קוד משותף בין חמשת המודולים ובין
הקיימים** — הכלל שכבר נקבע תקף שבעתיים כאן: הגיאומטריה של משולש ישר זווית אינה
דומה לגיאומטריה של רצועת אחוזים.

### Affected Files / Components

| קובץ | מה משתנה |
|---|---|
| `src/data/geometryShape.ts` | **חדש.** `geometryShape(question): GeometryShape \| null` — מלבן, ריבוע, משולש, מעגל. |
| `src/components/GeometryShape.tsx` | **חדש.** SVG: אחת מארבע הצורות, מידות על הצלעות. |
| `src/data/pythagorasTriangle.ts` | **חדש.** `pythagorasTriangle(question): PythagorasTriangle \| null`. |
| `src/components/PythagorasTriangle.tsx` | **חדש.** SVG: משולש ישר זווית בפרופורציה אמיתית. |
| `src/data/percentStrip.ts` | **חדש.** `percentStrip(question): PercentStrip \| null`. |
| `src/components/PercentStrip.tsx` | **חדש.** SVG: רצועה אחת, מלאה או עם הרחבה. |
| `src/data/ratioStrips.ts` | **חדש.** `ratioStrips(question): RatioStrips \| null`. |
| `src/components/RatioStrips.tsx` | **חדש.** SVG: שתי רצועות באותו גודל יחידה. |
| `src/data/linearGraph.ts` | **חדש.** `linearGraph(question): LinearGraph \| null`. |
| `src/components/LinearGraph.tsx` | **חדש.** SVG: צירים, ישר אחד או שניים, נקודה מסומנת. |
| `src/components/Practice.tsx` | חמישה `figure` נוספים, בסדר: קיים → geometryShape → pythagorasTriangle → percentStrip → ratioStrips → linearGraph → שלבים. חמישה כיתובים נוספים לרצף ההקראה. |
| `src/App.css` | `.geometry-figure`/`.geometry-shape`, `.pythagoras-figure`/`.pythagoras-triangle`, `.percent-figure`/`.percent-strip`, `.ratio-figure`/`.ratio-strips`, `.linear-figure`/`.linear-graph`, וסגנונות משותפים. |
| `tests/e2e/content.spec.ts` | חמישה כללים סופרים חדשים, בהתאמה למספרים שיימדדו בפועל (ראו "הספירה" למטה) — עבודת QA. |

### Data / State Changes

```ts
// geometryShape.ts
export type GeometryShape =
  | { kind: "rectangle"; length: number; width: number; unknown: "length" | "width" | null; measure: "area" | "perimeter"; caption: string }
  | { kind: "square"; side: number; measure: "area" | "perimeter"; caption: string }
  | { kind: "triangle"; base: number; height: number; unknown: "height" | null; measure: "area" | "perimeter"; caption: string }
  | { kind: "circle"; radius: number; pi: number; measure: "area" | "perimeter"; caption: string };

// pythagorasTriangle.ts
export interface PythagorasTriangle {
  legA: number;
  legB: number;
  /** Only carries a value when it is given or asked; a side that is neither is drawn
   *  unlabeled rather than showing an irrational number. */
  hyp: number | null;
  unknown: "legA" | "legB" | "hyp" | "bothLegs";
  caption: string;
}

// percentStrip.ts
export interface PercentStrip {
  /** The 100% reference amount. */
  base: number;
  /** How much of the strip is coloured, out of `base`. For a two-part discount
   *  question this is the discount amount specifically, whichever the question asks
   *  for — see Technical Approach. */
  filled: number;
  /** Set only for a price increase: how far the bar extends past `base`. */
  extra?: number;
  caption: string;
}

// ratioStrips.ts
export interface RatioStrips {
  /** Block counts, reduced to lowest terms. */
  ratioA: number;
  ratioB: number;
  /** What one block is worth. valueA = ratioA × unit, valueB = ratioB × unit. */
  unit: number;
  valueA: number;
  valueB: number;
  /** Two lines — one strip each, the same split `numberLine.ts` uses and for the same
   *  reason: two things to say, spoken with a pause between them. */
  caption: string[];
}

// linearGraph.ts
export interface LinearGraph {
  lines: { slope: number; intercept: number }[];
  /** The marked point — coordinates that reproduce `question.answer`. */
  point: { x: number; y: number };
  xMin: number;
  xMax: number;
  caption: string;
}
```

אין state חדש ב-`Practice` — כמו כל צורה קודמת, כל חמשת אלה נגזרים מ-`question`
בכל רינדור.

### Technical Approach

#### `geometryShape.ts` — ארבע צורות, שנים עשר ניסוחים, `30`/`30`

הנתונים בפועל (`src/data/grade6.ts`, נושא `geometry`) מתפצלים לשנים עשר ניסוחים
קבועים:

| # | ניסוח | צורה | `measure` | מחושב |
|---|---|---|---|---|
| 1 | `שטח מלבן שאורכו A ורוחבו B` | מלבן | area | `A×B` |
| 2 | `היקף מלבן שאורכו A ורוחבו B` | מלבן | perimeter | `2×(A+B)` |
| 3 | `שטח ריבוע שצלעו S` | ריבוע | area | `S²` |
| 4 | `היקף ריבוע שצלעו S` | ריבוע | perimeter | `S×4` |
| 5 | `שטח משולש שבסיסו B וגובהו H` | משולש | area | `B×H÷2` |
| 6 | `היקף משולש שווה צלעות שצלעו S` | משולש | perimeter | `S×3` |
| 7 | `היקף מעגל שרדיוסו R, כאשר פאי שווה P` | מעגל | perimeter | `2×P×R` |
| 8 | `שטח מעגל שרדיוסו R, כאשר פאי שווה P` | מעגל | area | `P×R²` |
| 9 | `היקף ריבוע הוא P. מה השטח שלו?` | ריבוע | area | `side=P÷4`, `area=side²` |
| 10 | `שטח מלבן הוא A ורוחבו W. מה האורך?` | מלבן | area, `unknown:"length"` | `length=A÷W` |
| 11 | `שטח מלבן הוא A ואורכו L. מה הרוחב?` | מלבן | area, `unknown:"width"` | `width=A÷L` |
| 12 | `שטח משולש הוא A ובסיסו B. מה הגובה?` | משולש | area, `unknown:"height"` | `height=A×2÷B` |

**כלל ה-`?`:** מסומן `?` רק על צלע/גובה שהוא **בעצמו** התשובה (ניסוחים `10`–`12`).
כששטח או היקף עצמם הם התשובה (ניסוח `9`, ורוב `1`–`8`), כל הצלעות מסומנות במספר
האמיתי שלהן — גם כשהוא מחושב ולא כתוב במפורש בניסוח (למשל צלע הריבוע בניסוח `9`)
— כי המילוי מול הקו המקיף כבר אומר על מה השאלה, בדיוק כמו שהוכרע ב-`design.md`.

**האימות:** בכל ניסוח, המספר המחושב חייב לשחזר את `question.answer`. **בנוסף**,
בניסוחים `9`,`10`,`11`,`12` יש בדיקת שלמות (`Number.isInteger`) לפני ההצגה — צלע
לא-שלמה מבטלת את הציור.

**ריבוע מוצג עם ארבע צלעות שוות**, לא כמלבן עם `length===width` — `kind: "square"`
נפרד כך שהרכיב מצייר אותו כריבוע גם ויזואלית (ש"כל הצלעות שוות" חלק מהצורה עצמה,
לא רק מהמספרים).

**מעגל:** `pi` נקרא מהניסוח (`כאשר פאי שווה 3`) ולא קבוע בקוד — אם ערך אחר יופיע
בעתיד בנתונים, החישוב עדיין נכון.

#### `pythagorasTriangle.ts` — משולש ישר זווית, `27`/`30`

שישה ניסוחים ממפים לאותה גיאומטריה — משולש עם שני ניצבים ויתר, שהיחס ביניהם
`a²+b²=c²`:

| ניסוח | מה נתון | `unknown` |
|---|---|---|
| `הניצבים הם A ו-B. מה אורך היתר?` | `legA=A`, `legB=B` | `"hyp"`, `hyp=√(A²+B²)` |
| `היתר הוא H וניצב אחד הוא A. מה הניצב השני?` | `hyp=H`, `legA=A` | `"legB"`, `legB=√(H²−A²)` |
| `אלכסון של מלבן הוא D וצלע אחת S. מה הצלע השנייה?` | אותה מתמטיקה, ניסוח שונה | `"legB"` |
| `סולם באורך H נשען על קיר, וקצהו העליון בגובה A. כמה רגלו רחוקה מהקיר?` | אותה מתמטיקה, ניסוח שלישי | `"legB"` |
| `היקף מלבן הוא P ואורכו L. מה אורך האלכסון?` | `legA=L`, `legB=P÷2−L` (נגזר) | `"hyp"` |
| `שני הניצבים שווים ושטח המשולש A. מה אורך כל ניצב?` | `legA=legB=√(2×A)` | `"bothLegs"` — שני הניצבים `?`, לא אחד |

**היתר אינו תמיד מחושב.** בניסוח האחרון (ניצבים שווים משטח) אין ליתר תפקיד
בשאלה — הוא לא נתון ולא מבוקש — ולכן `hyp: null` והצלע מצוירת בלי מספר עליה כלל.
תווית מופיעה רק על צלע שהיא נתונה (מגיעה מהניסוח) או מבוקשת (מסומנת `?`); צלע
שהיא לא זה ולא זה נשארת בלי כיתוב.

**מודגש:** `m5` ("קירוב שנתון בשאלה") ו-`h5`,`h10` ("בכמה קצר יותר ללכת באלכסון" —
התשובה **הפרש** בין שני מסלולים, לא צלע במשולש) פשוט אינם תואמים אף אחד משישה
הניסוחים — אין צורך בקוד חריג שמוציא אותם, הם נופלים דרך כל הבדיקות ומחזירים
`null` מעצמם.

**האימות:** המספר המחושב עבור `unknown` חייב לשחזר את `question.answer`, **וגם**
`Number.isInteger` על כל צלע מחושבת.

**הפרופורציה אמיתית** (הוכרעה ב-`design.md`) פירושה שהרכיב מצייר לפי האורכים
בפועל, מוקטן כך שהניצב הארוך תופס אורך קבוע — לא שלושה גדלים קבועים מראש. `5`,`12`
ייראה צר וגבוה במכוון.

#### `percentStrip.ts` — רצועה אחת, שני מבנים, `30`/`30`

חמישה ניסוחים, אבל רק **שני מבנים ויזואליים**: רצועה רגילה (מלאה עד `base`),
ורצועה שממשיכה מעבר ל-`base` (עליית מחיר).

| ניסוח | מבנה | `base` | `filled` | `extra` |
|---|---|---|---|---|
| `כמה זה X% מ-Y?` | רגיל | `Y` | `Y×X÷100` | — |
| `מחיר B שקלים בהנחה של X%. כמה שקלים ההנחה?` | רגיל | `B` | `B×X÷100` (=ההנחה) | — |
| `מחיר B שקלים בהנחה של X%. כמה משלמים?` | רגיל | `B` | `B×X÷100` (=ההנחה, לא התשלום) | — |
| `מחיר B שקלים עלה ב-X%. מה המחיר החדש?` | מורחב | `B` | `B` | `B×X÷100` |
| `P מתוך B. כמה אחוזים זה?` | רגיל | `B` | `P` | — |

**שני ניסוחי ההנחה תמיד מסמנים את `filled` כסכום ההנחה עצמה**, גם כששואלים "כמה
משלמים" — לפי ההכרעה ב-`design.md` (סעיף `5`, "לרצועה יש שני חלקים, והם נכונים
בו-זמנית"), הכיתוב אומר את שניהם: כמה ההנחה, וכמה נשאר. הרצועה מציגה תמיד את
שניהם; השאלה קובעת רק על מה מדבר המשפט הראשון בכיתוב.

**האימות:** `filled` (או `base+extra` לניסוח הרביעי, או האחוז המחושב לניסוח
החמישי) חייב לשחזר את `question.answer`. **וגם** `Number.isInteger` על כל מספר
מחושב.

**קווי העזר** כל `25%` מ-`design.md` מצוירים ביחס ל-`base`, גם ברצועה המורחבת —
כך `extra` נראה כחריגה אמיתית מעבר ל-`100%`, לא כהמשך רגיל של אותה סקאלה.

#### `ratioStrips.ts` — שתי רצועות, יחס מצומצם, `27`/`30`

חמישה ניסוחים שונים לגמרי לשונית, שכולם מצטמצמים לאותה תוצאה: יחס בין שני מונחים
(`ratioA:ratioB`), וערך אמיתי לכל צד (`valueA`,`valueB`) שמקיימים
`valueA/ratioA = valueB/ratioB = unit`.

| ניסוח | `ratioA:ratioB` (לפני צמצום) | איך `unit` נקבע |
|---|---|---|
| `היחס הוא A ל-B. אם החלק הראשון הוא P, כמה השני?` / `ביחס A ל-B, החלק הראשון הוא P...` | `A:B` | `unit=P÷A` |
| `יש A [יחידה1] לכל [יחידה2]. יש N [יחידה2]. כמה [יחידה1]?` (וכיוון הפוך) | `A:1` | `unit=N` |
| `היחס הוא A ל-B והסך הכל C. כמה בחלק הגדול/קטן?` | `A:B` | `unit=C÷(A+B)` |
| `היחס הוא A ל-B וההפרש בין החלקים הוא D. כמה בחלק הגדול?` | `A:B` | `unit=D÷|B−A|` |
| `מתכון ל-A [יחידה]. דורש B [יחידה]. כמה ל-C?` | `A:B` | `unit=C÷A` |

**היחס תמיד מצומצם למחלק המשותף הגדול ביותר** לפני הציור — לא רק בניסוח המתכון
(שם `4:2` היה נותן שמונה עיגולים לחינם), אלא בכולם, לאחידות. ברוב הניסוחים זה
לא-פעולה (`2:3` כבר מצומצם); רק במתכון זה משנה משהו בפועל.

**האימות:** `valueA` ו-`valueB` שניהם חייבים לצאת שלמים, **ואחד מהם** — זה שהשאלה
מבקשת — חייב לשחזר את `question.answer`.

**`h5`,`h9` (יחס הפוך — יותר עובדים, פחות ימים) ו-`h10` (יחס משולש)** אינם תואמים
אף אחד מחמשת הניסוחים ומחזירים `null` מעצמם — ההחלטה כבר רשומה ב-`product-spec.md`.

**הכיתוב מפוצל לשתי שורות** (`caption: string[]`), אותו טיפוס בדיוק שכבר נקבע
ל-`numberLine.ts` מסיבה זהה — שתי רצועות, ולכל אחת יש מה לומר.

#### `linearGraph.ts` — ישר על צירים, `30`/`30`

שישה ניסוחים, שכולם מצטמצמים לציור אחד: קו אחד או שניים על צירים, עם נקודה
מסומנת.

| ניסוח | מה מסומן |
|---|---|
| `בפונקציה y=Mx+B, מה הערך של y כאשר x שווה N?` | נקודה `(N, y)`, `y` מחושב |
| `מה החותך עם ציר y בישר y=Mx+B?` | נקודה `(0, B)` — **אותו מקרה בדיוק** כמו הקודם, עם `x=0` |
| `מה השיפוע של הישר y=Mx+B?` | שתי נקודות: `(0,B)` ו-`(1,B+M)`, בלי לחשב ערך שלישי |
| `בפונקציה y=Mx+B, מה הערך של x כאשר y שווה N?` (כולל `y=0`) | נקודה `(x, N)`, `x` מחושב |
| `הישר y=Mx+B עובר בנקודה שבה y שווה N. מה הערך של x?` | אותה מתמטיקה, ניסוח שונה |
| `שני ישרים נפגשים: y=... ו-y=.... מה ה-x בנקודת המפגש?` | שני קווים, נקודת המפגש מסומנת |

**"החותך עם ציר y" הוא מקרה פרטי של "ערך y כאשר x=0"** — לא ניסוח נפרד בקוד, רק
ערך `x` קבוע.

**חלון הצירים** נגזר מהנקודה המסומנת (ומשתי הנקודות, בשאלת שיפוע ומפגש) ומורחב
מעט משני הצדדים — אותו עיקרון בדיוק כמו חלון `numberLine.ts`, לפי ההכרעה שכבר
התקבלה ב-`design.md` ("אזהרה 2 נדחתה"). אין סקאלה קבועה בין שאלות.

**האימות:** הקואורדינטה המחושבת (`y` או `x`, לפי הניסוח) חייבת לשחזר את
`question.answer`. בשאלת מפגש, שני הישרים חייבים לחתוך בנקודה עם `x`,`y` שלמים
**וגם** `x` חייב לשחזר את `question.answer`.

### Edge Cases

- **מעגל, `pi` שונה מ-`3`.** לא קיים בנתונים היום, אבל `pi` נקרא מהניסוח ולא קבוע
  בקוד — שאלה עתידית עם `π=3.14` תעבוד בלי שינוי.
- **`h4`/`h9` בפיתגורס — שני ניצבים לא ידועים בו-זמנית.** היחיד מבין שבע הצורות
  שבו `?` מופיע פעמיים על אותו ציור. מוצדק: השאלה עצמה אינה נותנת אף ניצב, רק
  שטח ושוויון ביניהם.
- **יחס: `A:1` בניסוח "לכל".** כיוון המיפוי (מי `A` ומי `1`) תלוי במילה שאחרי
  "לכל" — "`2` בנים **לכל** בת" שם `בת` בצד ה-`1`. טעות כיוון הופכת כפל לחילוק.
  שתי הדוגמאות היחידות בנתונים (`e5`,`e10`) שתיהן נבדקות ידנית מול המסך לפני
  שהמימוש נחשב גמור.
- **פונקציה קווית, שיפוע שלילי.** `y=−4x+3` — הישר יורד משמאל לימין. אין טיפול
  מיוחד: שתי הנקודות `(0,3)` ו-`(1,−1)` פשוט יושבות כך שהקו יורד, והצירים
  מתרחבים לכלול `y` שלילי כשצריך.
- **כיווניות:** `direction: ltr` על כל חמשת ה-`<svg>` ועל כל `<text>` בתוכם —
  אותה מלכודת בדיוק שחוזרת בכל צורה, ואותו תיקון.

### Risks / Tradeoffs

- **הכיתובים לניסוחים ההפוכים/הנגזרים** (ריבוע מהיקף, פיתגורס לפי ניצב, שיפוע,
  אחוזים דו-חלקיים) **לא נכתבו במפורש ב-`design.md`** — הדוגמה היחידה שם לכל צורה
  היא הניסוח הישיר. הכיתובים כאן הם הרחבה מכנית של אותו כלל שכבר נקבע ("מה רואים,
  לא מה החשבון"), לא המצאה של קריטריון חדש — אבל הם לא עברו עין אנושית כמו
  הכיתובים המקוריים. **צפוי שהריוויו יתקן ניסוח**, בדיוק כמו שקרה בגל א׳.
- **הספירה בטבלאות למעלה נמדדה ידנית מול קובצי הנתונים, לא הורצה עדיין דרך קוד.**
  שלוש פעמים קודמות שמספר כזה נכתב לפני שנבדק בקוד הוא התברר לא מדויק
  (`179→198`, `20→25`, ותיקון בטבלת הספק גופה). **תיקון אחד כבר נמצא כאן:**
  `product-spec.md` מציין `26/30` לפיתגורס אבל מונה במפורש רק שלוש שאלות יוצאות
  (`m5`,`h5`,`h10`) — חשבון נכון על שלוש יוצאות מתוך `30` הוא `27`, לא `26`. אימוץ
  `27` כמספר הנכון; ה-`26` בספק הוא טעות חשבון, לא הכרעת תוכן חדשה. QA סופר בפועל
  מול הקוד ולא סומך על המספר הזה.
- **`GeometryShape` הוא union מתויג בעל ארבע צורות, לא ממשק שטוח אחד.** נבחר על
  פני שדה `sides: number[]` גנרי כי כל צורה צריכה גיאומטריה שונה לגמרי ברכיב
  (מלבן/ריבוע הם מלבן, משולש הוא שלוש נקודות, מעגל הוא רדיוס) — הבחנה בזמן
  קומפילציה עדיפה על ענפים ב-runtime.
- **`ratioStrips.ts` הוא המודול המורכב מכולם מבחינת ניתוח טקסט** — חמישה ניסוחים
  שונים לגמרי לשונית לאותה תוצאה. הוחלט במודע לא לפצל למספר מודולים (כמו ההבחנה
  `tenFrame`/`twentyStrip`) כי כולם מייצרים בדיוק את אותו טיפוס נתונים ואת אותו
  ציור — הפיצול שם היה כי הצורות **שונות**; כאן הן זהות וזה רק הטקסט שמשתנה.

### Open Questions

None.
