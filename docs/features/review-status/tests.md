# סטטוס ריוויו — Tests

## Coverage
`tests/e2e/review-status.spec.ts` — **7 בדיקות, כולן עוברות:**
- "נושא בדוק מתנהג כמו היום" → `grade 1 is reviewed and behaves exactly as before`
- "נושא לא בדוק מוצג, מעומעם ולא לחיץ" +  "טקסט קצר שמבינים" →
  `an unreviewed topic is shown but disabled, and says so`
- "לחיצה לא עושה דבר" → `clicking a blocked topic does nothing at all`
- מ-design.md, `disabled` אמיתי ולא עמעום →
  `a blocked card cannot be reached with the keyboard either`
- "כיתה שכולה חסומה מסבירה את עצמה" → `a grade with nothing reviewed explains itself`
- "ההיסטוריה נשמרת ונגישה" →
  `history stays reachable for a student whose topics are all blocked`
- "מצב הפתיחה: א׳ בדוק" → `grade 1 keeps working end to end while other grades are blocked`

## Status
🔴 **חסום. 7 חדשות עוברות, 15 קיימות נפלו.**

## מה נשבר, ולמה זו לא תקלת בדיקות

| קובץ | נפלו | מה אבד |
|---|---|---|
| `topics-all-grades.spec.ts` | 8 | מבנה הנושאים של ו׳/ח׳, 5 שאלות לרמה, ניווט, היסטוריה |
| `teaching-explanations.spec.ts` | 5 | **שלכל 330 השאלות יש הסבר, שלבים ודימוי ייחודי** |
| `students-and-syllabus.spec.ts` | 2 | כיווניות של ביטויים אלגבריים בכיתה ח׳ |

כל ה-15 ניגשות לתוכן של כיתות ו׳ וח׳ **דרך המסך**, והמסך עכשיו חוסם אותו.

**הן לא נכתבו לא נכון.** הן בודקות בדיוק את מה שהן צריכות לבדוק, ובדיוק הדרך
שבה הפרויקט הזה בודק תוכן מאז ומעולם. מה שהשתנה זו הנגישות אליו.

**וזו האירוניה שצריך לומר בקול:** הפיצ'ר נבנה כדי שתוכן לא בדוק לא יגיע לילדים.
התוצאה בפועל היא שהוא גם לא מגיע ל**בדיקות** — 180 השאלות של רותם ועומר מפסיקות
להיבדק אוטומטית בדיוק ברגע שבו הן הכי צריכות בדיקה. הגנה שמכבה את הגלאי.

**לא כתבתי מחדש את 15 הבדיקות כדי שיהיה ירוק.** זו הייתה מחיקה של כיסוי אמיתי
מתוך רצון לראות ✅.

## שלוש הדרכים לצאת מזה

**א. הבדיקות יקראו את הנתונים ישירות, לא דרך המסך.**
בדיקות תוכן — "לכל שאלה יש דימוי ייחודי", "5 שאלות לרמה" — לא צריכות דפדפן
מלכתחילה. הן נכונות יותר כקובץ שמייבא את `curriculum.ts` וסורק אותו, וגם ירוצו
בשניות במקום בדקות. **זו ההמלצה שלי:** היא פותרת את זה לתמיד, משפרת את מערך
הבדיקות, ולא מוסיפה שום חור למוצר.
*עלות:* כתיבה מחדש של ~10 בדיקות תוכן.

**ב. פתח מילוט לבדיקות** — פרמטר ב-URL שמתעלם מהחסימה.
*עלות:* חור אמיתי במוצר. מי שיודע את הפרמטר עוקף את החסימה, וזה בדיוק מה
שהפיצ'ר בא למנוע. **לא ממליץ.**

**ג. לסמן את ו׳ וח׳ כבדוקים ולוותר על החסימה עכשיו.**
*עלות:* הפיצ'ר לא עושה כלום עד שיהיה תוכן לא-בדוק חדש. **לא ממליץ** — זה
מבטל את מה שביקשת.

## How to run
`npm run test:e2e`
