# לומדים חשבון — הנחיות לסשן חדש

אפליקציית ווב בעברית לתרגול חשבון, לשלושה ילדים אמיתיים. **קראו את הקובץ הזה לפני
שנוגעים בקוד.**

חי ב-https://eyalzur.github.io/learn-math/ · React + TypeScript + Vite.

## התלמידים

| שם | כיתה | מה זה אומר |
|---|---|---|
| מיקה | א׳ (בת 7) | **עדיין לומדת לקרוא.** כל טקסט צריך להיות קצר, ורצוי שיהיה אפשר להקריא אותו |
| רותם | ו׳ | כיתה קטנה, אבל חומר של כיתה ו׳ רגילה |
| עומר | ח׳ | אלגברה, פיתגורס, פונקציות |

זה לא פרויקט הדגמה. החלטות מוצר נשפטות לפי "האם זה עוזר לילד/ה שכרגע טעו", לא לפי
מה שנוח לבנות.

## איך מתקשרים עם המשתמש
כותבים לו **בעברית** — תמיד, לא רק בתיעוד או בתוכן האפליקציה.

בקשת ריוויו היא **תמיד** לדף תצוגה מקדימה פשוט ועצמאי (כמו שכבר בונים לשינויים
שעוד לא באתר החי — ראו סעיף 3 למטה), **לא** הדרכה איך לנווט בתוך האפליקציה המלאה —
גם כששינוי כבר מוזג ונמצא באתר החי. המשתמש לא אמור לפתוח את האתר ולעבור כמה מסכים
כדי לבדוק דבר אחד; בונים לו עמוד ממוקד שמראה בדיוק את מה שצריך לשפוט.

## איך עובדים כאן

### 0. מתחילים סשן ב-`/status`
`/status` נותן בקצרה מה פתוח ובאיזה שלב — רעיון בלי ספק מוצרי, ספק בלי עיצוב,
מתוכנן וממתין לפיתוח, או נבנה וממתין לריוויו — ומה **חסום על המשתמש** ולא עלינו.
הוא קורא את `docs/features/*/status.md` ואת הסעיף "מה פתוח" כאן, ולכן הוא מדויק כל
עוד שני אלה מעודכנים. **זו הסיבה שכל שלב ב-pipeline מעדכן `status.md`.**

הפקודה נטענת בתחילת סשן, ולכן היא זמינה מהסשן **הבא** אחרי שהיא נכנסה — לא באותו
סשן שבו נכתבה.

### 1. כל שינוי עובר דרך ה-pipeline
יש שישה skills ב-`.claude/skills/`. הפעילו `/feature <תיאור>` וזה מריץ ברצף:
`product-manager` → `designer` → `tech-lead` → `developer` → `qa`.

כל שלב כותב מסמך ב-`docs/features/<slug>/` ועושה קומיט משלו, כך שההיסטוריה של ה-PR
נקראת כמו הסיפור של הפיצ'ר. **אל תדלגו על שלבים** — התיעוד הוא איך המשתמש (לא מתכנת)
עוקב אחרי מה קורה.

הכללים המשותפים ב-`.claude/skills/_shared/references/`. קראו אותם.

### 2. כל פיצ'ר בבראנץ' משלו עם PR
`main` **נפרס אוטומטית לאתר החי**, ולכן push ישיר אליו מפרסם קוד שאף אחד לא בדק.
`feature/<slug>` או `fix/<slug>`, ורק המתאם פותח PR — אחרי שהבדיקות ירוקות.
**לא ממזגים בלי אישור מפורש של המשתמש.**

### 3. כשמבקשים ריוויו — איפה, איך, ומה לצפות. בלי יוצא מן הכלל
המשתמש אינו מתכנת ואינו יודע לאן ללכת. **"צריך את הריוויו שלך על X" אסור.** זה חזר
שלוש פעמים בסשן אחד, ובכל פעם השאיר אותו בלי כלום.

**כל** בקשת ריוויו — גם על PR של תיעוד, גם על שורה אחת, גם כשהיא הרביעית באותה
הודעה — חייבת לכלול ארבעה שדות:

> **איפה:** [לינק לחיץ]
> **איך:** רותם ← שברים עשרוניים ← קל ← שאלה 3 (`0.4 + 0.2`) ← הקלד `6`
> **מה אמור לקרות:** "הספרות נכונות. אבל `0.4` ו-`0.2` שניהם קטנים מ-`1`…"
> **מה לשפוט:** האם הניסוח מלמד, או רק מודיע

**שלוש טעויות שכבר נעשו וחוזרות:**
- **"ראה בטבלה למעלה"** — הפניה אינה הוראה. חוזרים על העיקר בשורת הסיכום.
- **PR שני שנזכר בלי הוראות** — "ועוד #30 ממתין" אינו בקשת ריוויו. או שיש ארבעה
  שדות, או שלא מזכירים אותו כדבר שדורש ריוויו.
- **כלל שנכתב ל-PR שממתין לאישור** — הוראה שלא מוזגה אינה בתוקף. כשהמשתמש מבקש
  לתקן התנהגות, מתקנים וממזגים, לא מבקשים אישור על התיקון.

ל-PR של תיעוד אין מסך, ולכן **איפה** הוא קישור לקובץ, **איך** הוא איזה סעיף לקרוא,
ו**מה לשפוט** הוא השאלה שהמשתמש אמור לענות עליה.

ואם השינוי עדיין לא באתר החי — **בונים גרסת בדיקה ונותנים לינק**, במקום לתאר
במילים. `npm run build`, מטמיעים את ה-CSS וה-JS ב-HTML אחד, ומפרסמים.

### 4. מריצים בדיקות לפני שאומרים "מוכן"
```bash
npm run build && npm run lint && npm run test:e2e
```
כל הבדיקות צריכות לעבור (`299` נכון ל-2026-08-29). Playwright כבר מוגדר; הדפדפן
ב-`/opt/pw-browsers/chromium`.

## מלכודות שכבר עלו ביוקר

### כיווניות (RTL) — הבאג החוזר של הפרויקט
העמוד עברי ו-RTL, אבל חשבון חייב להיקרא שמאל-ימין. זה נשבר כאן **שלוש פעמים**:

1. `2 + 3 =` הוצג הפוך → `.problem-box` קיבל `direction: ltr`.
2. `3(x + 2)` בתוך משפט עברי הוצג `3)2 + x(` → הגדרת כיוון **לא מספיקה**, נדרש
   `unicode-bidi: isolate`. ביטויים בתוך משפט מסומנים בגרשיים אחוריים בנתונים
   ומפוצלים ב-`promptSegments`.
3. `20 ÷ 2 = 10` נכתב בשדה הטקסט במקום בשדה החשבוני והוצג `10 = 2 ÷ 20` →
   `normalizeStep` מעביר חשבון אוטומטית לשדה הנכון.

**הכלל:** טקסט עברי וביטוי חשבוני הם תמיד שני אלמנטים נפרדים. אל תסמכו על אלגוריתם
ה-bidi של הדפדפן על מחרוזת מעורבת. יש בדיקות שתופסות רגרסיה — אל תעקפו אותן.

### אותיות סופיות בעברית
`בלון` אינו תת-מחרוזת של `בלונים` (נ סופית מול נ רגילה). חיפוש מחרוזות על עברית
שביר. בדיקה שנשענה על זה נזרקה.

### שתי ריצות מקבילות של הסוויטה אינן שתי מדידות
`playwright.config.ts` מגדיר `reuseExistingServer`, ולכן שתי ריצות של `npm run test:e2e`
בו-זמנית חולקות שרת אחד ודורכות זו על זו. זה קרה ב-2026-08-15: ריצות מקבילות דיווחו
`14`, `40`, `46` ו-`62` כשלים על אותו קוד בדיוק, ואחת מהן דיווחה **אפס** — והמסקנה
שנשענה עליה ("רק `5` קבצים נשברו") הייתה שגויה. ריצה בודדת ונקייה נתנה את התמונה
האמיתית.

**הכלל: מריצים את הסוויטה פעם אחת, ומחכים.** אם דיווח נשען על ריצה שהתחילה בזמן
שריצה אחרת עוד רצה — הוא לא נמדד, הוא נוחש.

### תלות מעגלית בין קבצי נתונים
`grade1.ts` קרא ל-`level()` מ-`curriculum.ts` שמייבא אותו בחזרה → מסך לבן,
`Cannot access 'LEVEL_META' before initialization`. TypeScript **לא תופס** את זה.
לכן `level()` יושב ב-`src/data/level.ts` בנפרד. קובץ תוכן חדש לכיתה — ייבאו משם.

## מבנה

```
src/data/
  curriculum.ts   התלמידים, הכיתות, הטיפוסים. Grade הוא איחוד מבחין:
                  practice: "topics" (מיקה) או "levels" (רותם, עומר)
  grade1.ts       150 השאלות של כיתה א׳, לפי נושא
  level.ts        LEVEL_META ו-level() — בנפרד בגלל התלות המעגלית
  explain.ts      בונה את ההסבר: שלבים כתובים, או מחושבים מהאופרנדים
  progress.ts     היסטוריית תרגולים ב-localStorage
  speech.ts       הקראה בקול + המרת חשבון למילים עבריות
src/components/   StudentPicker · TopicPicker · LevelPicker · Practice · Result · History
docs/features/    מסמך לכל פיצ'ר. README.md הוא האינדקס
```

**לכל שאלה חייב להיות `analogy`** — שדה חובה, כך שהקומפיילר מסרב לשאלה בלי דימוי
מהחיים. זה מכוון: זה קריטריון קבלה שאחרת נשען על זיכרון.

## מה פתוח

### ממתין לריוויו ולמיזוג
אין כרגע אף PR שממתין.

**מוזג היום (2026-08-29):** [#55](https://github.com/eyalzur/learn-math/pull/55)
שרת המחברת — שלב א׳: הפיצ'ר הראשון בפרויקט עם **רכיב שרת אמיתי**. `server/`
(Node+Express+pngjs) מקבל את מטריצת התאים של דף מחברת ומחזיר תמונת PNG; כפתור
"שלח למורה" חדש במחברת שולח את הדף ומציג בחזרה את מה שהשרת קיבל. אין קריאה
ל-Claude בשלב הזה — זו הוכחת תשתית בכוונה. **השרת נפרס בפועל באותו יום** ל-Cloud
Run (`https://notebook-server-864901299725.me-west1.run.app`, פרויקט
`learn-math-506923`, region `me-west1`) והכתובת חוברה ל-build של האתר, ולכן זה
עובד מקצה לקצה **באתר החי**. ראו `server/README.md` לפריסה מחדש ולשני המכשולים
החד-פעמיים שנתקלנו בהם. · [#56](https://github.com/eyalzur/learn-math/pull/56)
פריסה אוטומטית של השרת — כל push ל-`main` שנוגע ב-`server/` נפרס לבדו ל-Cloud
Run דרך GitHub Actions, בלי סוד כלשהו בריפו (Workload Identity Federation). המשתמש
ביצע את ההגדרה החד-פעמית בגוגל קלאוד לפני המיזוג. · [#57](https://github.com/eyalzur/learn-math/pull/57)
תיקון ניסוח — כותרת מסך "שלח למורה" שינתה מ"זה מה שהשרת קיבל" (מונח תשתית, לא
מתאים לילד/ה) ל"זה הדף שהמורה מסתכל עליו". 299/299 בדיקות. שלושתם **באתר החי**.

**מוזג ב-2026-08-21:** [#51](https://github.com/eyalzur/learn-math/pull/51)
שיעור לעומת תרגול — מסך לימוד לכל נושא, כפתור 📖 משני על כרטיס נושא. עבר שני
סבבי רוויזיה אחרי תצוגה מקדימה: קודם כמה דוגמאות אצל רותם/עומר (אחת לכל דרגת
קושי, לא רק הקלה ביותר), ואז שינוי UI מגלילה על כל הדוגמאות לדפדוף עמוד-אחר-עמוד
("מחברת") — אצל מיקה נשארת דוגמה אחת ללא ניווט. 246/246 בדיקות עברו. **באתר החי**.
· [#50](https://github.com/eyalzur/learn-math/pull/50) פתיחת סוגריים בביטויים
אלגבריים — מציגה ביטוי פתוח ומגדירה מקדם/מספר חופשי, כולל שלושה סבבי תיקון אחרי
ריוויו תוכן (הצגת הביטוי הפתוח, ניסוח ה-hint וה-step של "כנסו איברים" כך
שמסבירים בלי לפתור). **באתר החי**.

**מוזג ב-2026-08-20:** [#48](https://github.com/eyalzur/learn-math/pull/48) רמות
הופכות לתרגול מסתגל — כל 12 נושאי רותם ועומר, כולל שני סבבי תיקון אחרי ריוויו תוכן
(איסור עשרוני בנתוני שאלה, דימויים שמסתיימים בשאלה, ניסוח רמזים, כפל בשבר, ציור המאונך
לעשרוניים) · [#49](https://github.com/eyalzur/learn-math/pull/49) רצועת האחוזים
מתחלקת לפי האחוז שבשאלה, וההסבר אומר למה — כולל שני תיקוני המשך אחרי ריוויו (שבר
כמספר, כיתוב הרצועה). שניהם **באתר החי**.

**הכל השאר מוזג ל-`main` ובאתר החי** — כולל #43–#47 (קושי מסתגל, אבחון סטייה-של-אחד,
כותרות נשברות, תוכן כיתה ב׳, ניקוי תיעוד). הרשימה המפורטת לפי תאריך מיזוג היא
`docs/features/README.md`, לא כאן — הטבלה הזו כבר התיישנה פעם אחת (החזיקה PR
שכבר מוזג) בגלל שני בראנצ'ים שערכו את הקטע הזה במקביל; **הכלל: כשמוזגים PR, מריצים
את סקיל `merge-pr`, לא עורכים את הטבלה הזו ביד.**

### הערות מהריוויו שטרם טופלו (2026-08-15)
- **הרמז ב-`במספר 18, בכמה גדולה ספרת היחידות…`** — המשתמש: "רמז לא מובן". הרמז
  (`משווים שתי ספרות: כמה בודדים יש, וכמה קופסאות מלאות`) אומר **להשוות** ולא אומר
  **לחסר**, וזו הפעולה. חל על `5` שאלות — סגנון `digitGap`, לא שאלה בודדת.
- **"השאלות של מיקה קלות מידי"** — נמדד: **המספר הגדול ביותר בכל `150` השאלות של
  כיתה א׳ הוא `20`**, ואין ולו מספר אחד מעליו. כיתה א׳ בישראל מגיעה ל-`100`.
- **"יש פער בנושאים" — שאלה פתוחה למשתמש, ולא לנחש אותה.** שתי קריאות אפשריות,
  ושתי עבודות שונות לגמרי: (א) פער **בתוך** כיתה א׳ — חסרים מספרים עד `100`, בעיות
  מילוליות, כסף, שעה; (ב) פער **בין הילדים** — מיקה עד `20` ורותם בשברים של כיתה ו׳,
  ואין ביניהן דבר.

### הערות מהריוויו שטרם טופלו (2026-08-18, קושי מסתגל — `PR #43`)
- **מהירות ההתקדמות בקושי צריכה להיות ניתנת לכיוון.** "יכול להיות שצריך פיצ'ר של
  מהירות התקדמות. כלומר כמה מהר המורה יעלה וירד בקושי." כרגע קבוע בקוד (`2`
  תשובות נכונות ברצף מעלות, `1` טעות מורידה מיד). ראו
  `docs/features/adaptive-difficulty/status.md`.
- **שתי שאלות זהות (אותם מספרים) עלולות לצאת ברצף.** "יצאו לי שני תרגילים זהים
  ברצף, אותם המספרים. זה לא צריך לקרות." סומן מראש כ-Edge Case "נחמד להיות" ולא
  יושם — עכשיו יש עדות בפועל. ראו `docs/features/adaptive-difficulty/status.md`.

### מתוכנן ומאושר, טרם נבנה
- **גל ב׳ של הסגנונות** — כיתות ו׳ ו-ח׳. המשתמש הכריע שהוא **יכתוב שאלות חדשות**
  למלא סגנונות דלילים ("בעיות מילוליות" אין בו אף סגנון עם `3` שאלות), ולכן זו
  עבודת תוכן ולא המשך של הסיווג. ראו `docs/features/style-lessons/product-spec.md`.
- **שמות קבועים לדמויות בדימויים** — `38` דימויים בשלוש הכיתות אומרים "ילד אחד",
  "לחבר", "שני תלמידים". **המשתמש אישר את השמות דניאל, יעל ונועם (2026-08-20)** —
  ממתין ל-`/feature` שיישם.

### עוד לא התחיל
- **אבחון קשיים לאורך זמן** — לזהות באיזה נושא התלמיד/ה נתקעים שוב ושוב ולהציע חיזוק
  ממוקד. אבחון של טעות **בודדת** כבר קיים ב-`src/data/diagnose.ts`; מה שחסר הוא המבט
  על פני תרגולים, שיישען על `progress.ts`.
- **מחברת בכתב יד (ציור)** — המשתמש תיאר (2026-08-14, עודכן 2026-08-21): מסך
  מלא **דמוי מחברת משבצות** שהתלמיד/ה כותב/ת עליו כמו על דף אמיתי, ו-LLM ("המורה")
  קורא את מה שנכתב ומסביר **מה הוא הבין שהתלמיד/ה עשה** — לא רק ניקוד ("קראתי 68 —
  נכון?"), אלא שיקוף של הבנת התהליך, כדי שהתלמיד/ה יוכל/תוכל לתקן את עצמו/ה אם ה"מורה"
  הבין לא נכון. **המשתמש ביקש לדייק את זה קודם**, ורק אחר כך להתקדם לשלב הבא: אינטרקציה
  דו-כיוונית בין התלמיד/ה למורה (לא רק תגובה חד-פעמית). זה הצרכן הטבעי הראשון של צד
  השרת שאושר — ראו "אילוצים".

### רק המשתמש יכול לעשות
- **הגנת בראנץ'.** `main` נפרס אוטומטית לאתר החי, אבל אין עדיין דרישה שהבדיקות יעברו
  לפני מיזוג. Settings → Branches → `main` → require `check` + `test`.
- **חיבור קלוד ↔ גוגל קלאוד.** התוכנית השתנתה: **לא** מפתח API סטטי ב-Secret
  Manager, אלא Workload Identity Federation — כמו שכבר נבנה בין GitHub Actions
  לגוגל קלאוד ב-`server-auto-deploy`. ה-service account שמצורף ל-Cloud Run מבקש
  OIDC identity token ישירות מ-Google, ו-Anthropic מאמת אותו מול `accounts.google.com`
  — **אין מפתח API בכלל**, ולא רק "לא בריפו". דרושים: יצירת service account ייעודי
  חדש (`notebook-server-claude`), פריסה מחדש של Cloud Run איתו, והרשמה של אותו
  service account ב-Anthropic Console ("Connect workload"). **המשתמש מבצע את זה
  כרגע בסשן נפרד** — טרם אושר סיום. תקרת ה-`$5` שכבר נרכשה תקפה גם תחת federation
  (אותה הקצאת שימוש כמו מפתח API).
- ~~**חשבון גוגל קלאוד.**~~ **בוצע (2026-08-29):** פרויקט `learn-math-506923`,
  חשבון חיוב מקושר, והשרת הראשון (`server/`) נפרס ל-Cloud Run ועובד.
- ~~**פריסה ידנית של השרת בכל שינוי.**~~ **בוצע (2026-08-29):** פריסה אוטומטית
  ל-Cloud Run בכל push ל-`main` שנוגע ב-`server/` — ראו
  `docs/features/server-auto-deploy/` ו-`server/README.md`. המשתמש גם ביצע את
  ההגדרה החד-פעמית הנדרשת בגוגל קלאוד (Workload Identity Federation, בלי סוד
  כלשהו) — הפריסה האוטומטית פעילה. הפריסה הידנית הישנה נשארת מתועדת ב-
  `server/README.md` כדרך חלופית.

## אילוצים

**האתר עצמו נשאר סטטי ב-GitHub Pages.** ההקראה ב-`speechSynthesis` המובנה, והציורים
וההסברים מחושבים בדפדפן.

**יש צד שרת, והוא חי (2026-08-29).** `server/` — Node+Express, נפרס ל-Google Cloud
Run בפרויקט `learn-math-506923`. כרגע הוא עושה דבר אחד בלבד: מקבל את מטריצת התאים
של דף מחברת ומחזיר תמונת PNG (`POST /render-page`). **אין בו עדיין שום קריאה
ל-Claude** — זו הייתה הכרעה מכוונת לבודד את התשתית לפני שמוסיפים LLM. הצרכן הבא
המתוכנן: קריאת דף המחברת בכתב יד. ראו `server/README.md` ו-
`docs/features/notebook-server-relay/`.

שני כללים שאינם משתנים: **מפתח API לעולם לא בדפדפן ולא בריפו**, ו**הילד לעולם לא
ממתין לשרת** — ההסבר הקיים מוצג מיד, והשרת רק משפר. הכלל השני אינו נשבר על ידי
פעולה שהתלמיד/ה יזם/ה במפורש (לחיצה על "שלח למורה") — הוא מדבר על מה שכבר עובד
בלי שרת.
