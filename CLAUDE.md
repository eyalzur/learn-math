# לומדים חשבון — הנחיות לסשן חדש

אפליקציית ווב בעברית לתרגול חשבון, לשלושה ילדים אמיתיים. **קראו את הקובץ הזה לפני
שנוגעים בקוד.**

חי ב-https://eyalzur.github.io/learn-math/ · React + TypeScript + Vite.

## התלמידים

| שם | כיתה | מה זה אומר |
|---|---|---|
| מיקה | א׳ (בת 7) | **עדיין לומדת לקרוא.** כל טקסט צריך להיות קצר, ורצוי שיהיה אפשר להקריא אותו |
| רותם | ו׳ | כיתה קטנה, אבל חומר של כיתה ו׳ רגילה |
| עומר | ח׳ | אלגברה, פיתגורס, פונקציות |

זה לא פרויקט הדגמה. החלטות מוצר נשפטות לפי "האם זה עוזר לילד/ה שכרגע טעו", לא לפי
מה שנוח לבנות.

## איך עובדים כאן

### 1. כל שינוי עובר דרך ה-pipeline
יש שישה skills ב-`.claude/skills/`. הפעילו `/feature <תיאור>` וזה מריץ ברצף:
`product-manager` → `designer` → `tech-lead` → `developer` → `qa`.

כל שלב כותב מסמך ב-`docs/features/<slug>/` ועושה קומיט משלו, כך שההיסטוריה של ה-PR
נקראת כמו הסיפור של הפיצ'ר. **אל תדלגו על שלבים** — התיעוד הוא איך המשתמש (לא מתכנת)
עוקב אחרי מה קורה.

הכללים המשותפים ב-`.claude/skills/_shared/references/`. קראו אותם.

### 2. כל פיצ'ר בבראנץ' משלו עם PR
`main` **נפרס אוטומטית לאתר החי**, ולכן push ישיר אליו מפרסם קוד שאף אחד לא בדק.
`feature/<slug>` או `fix/<slug>`, ורק המתאם פותח PR — אחרי שהבדיקות ירוקות.
**לא ממזגים בלי אישור מפורש של המשתמש.**

### 3. מריצים בדיקות לפני שאומרים "מוכן"
```bash
npm run build && npm run lint && npm run test:e2e
```
כל 38 הבדיקות צריכות לעבור. Playwright כבר מוגדר; הדפדפן ב-`/opt/pw-browsers/chromium`.

## מלכודות שכבר עלו ביוקר

### כיווניות (RTL) — הבאג החוזר של הפרויקט
העמוד עברי ו-RTL, אבל חשבון חייב להיקרא שמאל-ימין. זה נשבר כאן **שלוש פעמים**:

1. `2 + 3 =` הוצג הפוך → `.problem-box` קיבל `direction: ltr`.
2. `3(x + 2)` בתוך משפט עברי הוצג `3)2 + x(` → הגדרת כיוון **לא מספיקה**, נדרש
   `unicode-bidi: isolate`. ביטויים בתוך משפט מסומנים בגרשיים אחוריים בנתונים
   ומפוצלים ב-`promptSegments`.
3. `20 ÷ 2 = 10` נכתב בשדה הטקסט במקום בשדה החשבוני והוצג `10 = 2 ÷ 20` →
   `normalizeStep` מעביר חשבון אוטומטית לשדה הנכון.

**הכלל:** טקסט עברי וביטוי חשבוני הם תמיד שני אלמנטים נפרדים. אל תסמכו על אלגוריתם
ה-bidi של הדפדפן על מחרוזת מעורבת. יש בדיקות שתופסות רגרסיה — אל תעקפו אותן.

### אותיות סופיות בעברית
`בלון` אינו תת-מחרוזת של `בלונים` (נ סופית מול נ רגילה). חיפוש מחרוזות על עברית
שביר. בדיקה שנשענה על זה נזרקה.

### תלות מעגלית בין קבצי נתונים
`grade1.ts` קרא ל-`level()` מ-`curriculum.ts` שמייבא אותו בחזרה → מסך לבן,
`Cannot access 'LEVEL_META' before initialization`. TypeScript **לא תופס** את זה.
לכן `level()` יושב ב-`src/data/level.ts` בנפרד. קובץ תוכן חדש לכיתה — ייבאו משם.

## מבנה

```
src/data/
  curriculum.ts   התלמידים, הכיתות, הטיפוסים. Grade הוא איחוד מבחין:
                  practice: "topics" (מיקה) או "levels" (רותם, עומר)
  grade1.ts       150 השאלות של כיתה א׳, לפי נושא
  level.ts        LEVEL_META ו-level() — בנפרד בגלל התלות המעגלית
  explain.ts      בונה את ההסבר: שלבים כתובים, או מחושבים מהאופרנדים
  progress.ts     היסטוריית תרגולים ב-localStorage
  speech.ts       הקראה בקול + המרת חשבון למילים עבריות
src/components/   StudentPicker · TopicPicker · LevelPicker · Practice · Result · History
docs/features/    מסמך לכל פיצ'ר. README.md הוא האינדקס
```

**לכל שאלה חייב להיות `analogy`** — שדה חובה, כך שהקומפיילר מסרב לשאלה בלי דימוי
מהחיים. זה מכוון: זה קריטריון קבלה שאחרת נשען על זיכרון.

## מה פתוח

### ממתין למשתמש — שני PR ירוקים שלא מוזגו
- **[#22 ציור לשברים](https://github.com/eyalzur/learn-math/pull/22)** — עיגול מחולק
  בראש קופסת ההסבר, ל-24 שאלות השברים של כיתה ו׳. הציור נגזר מהניסוח ומוצג רק אם
  שחזר את `question.answer`.
- **[#23 תיקון "פספסת בדיוק באחד"](https://github.com/eyalzur/learn-math/pull/23)** —
  באג שדווח מהאתר החי. הפירוט ב-`docs/features/mistake-diagnosis/status.md`.

### בתכנון, ועצר לדיון במכוון
- **ציורים לשבעת הנושאים הנותרים** — מוצר ועיצוב כתובים ב-`docs/features/more-diagrams/`,
  בבראנץ' `feature/more-diagrams`. `tech-lead` ואילך **טרם רצו**, לפי בקשה מפורשת של
  המשתמש לעצור אחרי העיצוב. הבראנץ' מסתעף מ-`feature/fraction-diagram` ולכן יצטרך
  rebase על `main` אחרי שיימוזג #22.

### עוד לא התחיל
- **אבחון קשיים לאורך זמן** — לזהות באיזה נושא התלמיד/ה נתקעים שוב ושוב ולהציע חיזוק
  ממוקד. אבחון של טעות **בודדת** כבר קיים ב-`src/data/diagnose.ts`; מה שחסר הוא המבט
  על פני תרגולים, שיישען על `progress.ts`.
- **דף כתיבה חופשי + זיהוי כתב יד**, עם אישור לפני ניקוד ("קראתי 68 — נכון?").

### רק המשתמש יכול לעשות
**הגנת בראנץ'.** `main` נפרס אוטומטית לאתר החי, אבל אין עדיין דרישה שהבדיקות יעברו
לפני מיזוג. Settings → Branches → `main` → require `check` + `test`.

## אילוץ קשיח

**בלי שירותים בתשלום.** האתר סטטי ב-GitHub Pages, בלי שרת ובלי מפתחות API. כל דבר
חייב לרוץ בדפדפן של המשתמש. זה למה ההקראה משתמשת ב-`speechSynthesis` המובנה, ולמה
זיהוי כתב יד יצטרך לרוץ על המכשיר.
